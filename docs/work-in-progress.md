# Work In Progress

> **Purpose:** the active task ledger — absorbs all task-level churn so `CLAUDE.md`
> and `docs/history.md` are never touched mid-workstream. **One line per task, no
> narrative** — detail lives in commit messages and the workstream folder
> (`docs/ws1-sync-pipeline/`). Write each block in final
> archival form from the start: on completion or abandonment, move it verbatim to
> `docs/history.md` under a dated heading (mark abandoned blocks with the reason) and
> delete it here — completion is a copy-paste, never a rewrite. Multiple concurrent
> workstreams get separate blocks. Status marks: `[x]` done, `[>]` in progress, `[ ]`
> not started.

---

## WS1 — Sync Pipeline Rewrite
Started: 2026-08-04 | Status: active
Current focus: Task 5 — Rust integration for forced alignment (Phase 3). See `docs/ws1-sync-pipeline/ws1-master-roadmap.md`'s NEXT UP block.

- [x] 1. Apply Sync history-entry fix (Slice 1) — commit `1b16a50`
- [ ] 2. Slice 2 — re-derive the 50/50 silence-split rule (park-commit `210855d`, `snapBoundaries.ts` + Apply-Sync plumbing) against current `main`. Will deliberately break the golden replay — budget a per-boundary review, never a blind re-baseline of `scripts/fixtures/phase4-baseline-*-segments.csv`.
- [ ] 3. Verify stale-anchor scroll degradation with a dedicated test (currently asserted correct by code reading only, no test)
- [ ] 4. Word-shift defect — fix the 2 remaining v2-fixable cases (`does the same‖what the job`, `s youngest scout‖a girl of`); the 3rd (`seasons than you‖can count and`) is a script-vs-narration authority conflict, not fixable by any timing-source change. Evidence: `docs/ws1-sync-pipeline/boundary-drift-investigation.md`. Expected to resolve as a side effect of task 5's token-smear-cascade fix (`docs/ws1-sync-pipeline/roadmap-2026-08-07.md` §D2), not as standalone work — task 6's "blocked on task 4" therefore really means blocked behind task 5.
- [ ] 5. Rust integration for forced alignment — NOT BLOCKED — all three Rust gates are closed (Spanish accuracy via Step U, structural checks via Steps W/X, heading assignment via Option A). Per owner ruling R-B, the fr/de/pt unvalidated-language warning surfaces (Step T.7) ship in this same task/release, not after. CTC Viterbi forced-alignment DP ported to Rust, commit `6a0ac21`. **Slice D2 (2026-08-12): real ONNX forward pass wired into `fa_align` behind an OFF-by-default `fa-inference` Cargo feature** — `ort = "=2.0.0-rc.13"` (optional, `default-features = false`, `features = ["std", "load-dynamic"]`, no `api-NN` feature — keeps the onnxruntime minor-version floor at 17 per the ruling below) added to `src-tauri/Cargo.toml`; new `src-tauri/src/fa_onnx.rs` (WAV decode, zero-mean/unit-var normalization, ONNX session + forward pass via `ORT_DYLIB_PATH`, minimal placeholder vocab/tokenizer, wired into the existing `fa_viterbi` DP). New Python-reference fixtures (`scripts/fixtures/fa-onnx-emission-{en-deep-night,en-mother-look,es-resultan-inutiles}.json`, generated by new `scripts/capture-fa-onnx-reference.py`) prove Rust-`ort` emission parity against the REAL shipping jonatasgrosman ONNX models — the pre-existing `fa-emission-*.json` DP fixtures are MMS_FA-based and not comparable (different model/vocab), so this slice deliberately did not reuse them for the parity gate. Result: 0/577 argmax mismatches across all 3 fixtures (191+207+179 frames), max abs diff 0.0003–0.0012. Feature-off build/tests byte-identical to `6a0ac21` baseline (cargo test 31/31 unchanged); feature-on adds 18 tests (49 total). Gates: `cargo check` clean both configs, `npm run lint` clean, `npm test` 74 files/1857 passed/1 skipped (unchanged), golden replay 6/6 (unchanged). Cargo.lock diff is exactly the 10-package `ort` tree (ort, ort-sys, ndarray + its own transitive deps, libloading 0.9.0) — confirmed via `cargo tree -i ndarray` (empty; ort declares ndarray as an optional dep Cargo pins for future-toggle stability but never compiles here) and zero build artifacts, no unrelated crate touched. **Slice D3 (2026-08-12): vocab-aware FA text normalizer ported to Rust, replacing the D2 ASCII-only placeholder tokenizer.** New `src-tauri/src/fa/text.rs` — UNCONDITIONAL (not behind `fa-inference`), byte-identical port of `src/services/faTextNormalize.ts` (diacritic-preserving, German ß->ss, digit-bearing/unspellable-word drop, typographic fold/strip, zero-width stripping). Two deliberate, tested deviations from a literal port, both required because Rust's std lacks Unicode normalization and this slice's scope forbade new crates: (1) NFC composition is a hand-rolled ~54-entry table scoped to exactly the accented letters the five shipped vocabs contain (verified via `unicodedata.normalize('NFD', ch)` over each vocab's non-ASCII characters — all single-mark, no ligatures) rather than general Unicode NFC; (2) whitespace splitting reproduces ECMA-262's exact `\s` code point set (confirmed by direct Node testing: `"a﻿b".split(/\s+/)` splits on BOM, which Rust's `char::is_whitespace()` does not) rather than `str::split_whitespace()`. `fa_onnx.rs`'s `text_to_token_ids` now normalizes via `fa::text::normalize_for_forced_alignment` before mapping to vocab ids — the ASCII placeholder and its "drops individual unknown chars" behavior are gone (a digit-bearing or genuinely out-of-vocab word is now dropped WHOLESALE, matching the TS reference, not char-by-char). `serde_json` promoted from `fa-inference`-gated optional to a plain dependency (`text.rs` needs it unconditionally) — confirmed zero new `Cargo.lock` packages. New shared parity fixture `scripts/fixtures/fa-text-normalize-fixture.json` (36 corpus entries covering all 5 languages, diacritics, German ß/eszett, French loanword chars, digit-bearing words, pure punctuation, empty/whitespace-only input, mixed case, a genuine Cyrillic/CJK OOV case per the non-vacuity check, and a BOM-as-splitter edge case; 24 KB, generated by new `scripts/generate-fa-text-fixture.ts` by driving the live TS module, never hand-derived) — `text.rs`'s `fixture_parity` tests assert Rust matches it for every entry/language, and new `src/services/faTextNormalize.fixtureDrift.test.ts` asserts the fixture still matches the live TS module, so the two implementations can never silently drift apart. `src/services/faTextNormalize.ts`/`textNormalize.ts`/`canonicalize()` diff: empty (confirmed via `git diff --stat`), as required. Gates: `cargo check` clean both configs; `cargo test` feature-off 31→46 (+15, all `fa::text`); `cargo test --features fa-inference` 49→66 (+17: 15 `fa::text` + net +2 `fa_onnx` tokenizer tests after replacing the placeholder-specific ones); the 3 D2 ONNX argmax-parity tests still pass (skip cleanly, `ORT_DYLIB_PATH` unset in this environment, same as the D2 baseline — untouched code path); `npm run lint` clean; `npm test` 74→75 files, 1857→1894 passed (+37: 36 fixture entries + 1 coverage check), 1 skipped unchanged; golden replay 6/6 unchanged. **Slice D4 (2026-08-12): end-to-end alignment parity + skip hardening — closes two D3-found silent-failure channels, then proves the full Rust FA path matches a real Python reference end to end.** (a) Skip hardening: new `FA_REQUIRE_ORT=1` env flag (`fa_onnx.rs`'s `require_ort` module) makes a would-be skip (missing `ORT_DYLIB_PATH` or missing `model.onnx`) `panic!` instead of silently `eprintln!`-and-return; default (unset) behavior is bit-for-bit unchanged. Applied to all 3 D2 parity tests and the 3 new D4 tests below. Confirmed all three env combinations behave as required: unset/unset → clean skip, 0 failures; set+`FA_REQUIRE_ORT=1` → 0 skips, all execute; unset+`FA_REQUIRE_ORT=1` → hard failure naming what's missing. (b) Re-ran the D2 ONNX argmax-parity tests for real (`ORT_DYLIB_PATH` pointed at the `onnxruntime-osx-x86_64-1.23.2` dylib per the runtime-unblock doc, `FA_REQUIRE_ORT=1`) — **this is the first time they actually executed since D3's tokenizer swap; they were never confirmed green post-swap before now.** Result: 0/577 argmax mismatches across all 3 fixtures (191+207+179 frames), max abs diff 0.0003–0.0012 — same numbers as D2's original capture, confirming D3's tokenizer swap did not regress forward-pass parity. (c) NFC completeness guard (`fa/text.rs`'s new `nfc_completeness_guard` test): loads all 5 committed vocab fixtures, decomposes every non-ASCII vocab char against an INDEPENDENT 230-entry reference NFD table (generated fresh from Python `unicodedata` over Latin-1 Supplement + Extended-A + Extended-B, U+00C0–U+0250 — deliberately NOT derived from `NFC_COMPOSE`, so the two tables can't trivially agree by construction), and asserts every real decomposition is covered by `NFC_COMPOSE`. All 28 non-ASCII chars across the 5 vocabs check out. Non-vacuity proven by hand: temporarily deleted the `('e','́','é')` `NFC_COMPOSE` row — guard failed immediately, naming the exact 3 vocabs (es/fr/pt) and char affected; restored, re-verified green (`git diff --stat` on `text.rs` after: clean, pure addition). (d) End-to-end parity harness: new `scripts/generate-fa-e2e-tokens.ts` (drives the live, D3-proven `faTextNormalize.ts` to produce target token ids — the only new logic is a trivial char→id/word-delimiter mapping) + new `scripts/capture-fa-e2e-reference.py` (reuses D2's committed `input_samples`/`emission_log_probs` verbatim, runs REAL torchaudio 2.2.2 `forced_align`/`merge_tokens` — an independent C++ implementation of the same DP `fa_viterbi.rs` was hand-ported from, never touching Rust output) produce a new fixture family `scripts/fixtures/fa-e2e-alignment-{en-deep-night,en-mother-look,es-resultan-inutiles}.json` (23/28/22 target tokens and merged spans respectively) — does not read, modify, or overwrite `fa-emission-*.json`, `fa-onnx-emission-*.json`, or `fa-text-normalize-fixture.json`. New `fa_onnx.rs::e2e_parity` module runs the COMPLETE Rust path (resolve model → zero-mean/unit-var normalize → real `ort` forward pass → `fa::text` tokenize → `fa_viterbi::forced_align` → `merge_tokens`) against that fixture. **Result: 0 divergences — every merged span's start/end frame is bit-for-bit IDENTICAL to the torchaudio reference across all 3 fixtures and all 73 combined target tokens/spans (23+28+22), zero tolerance, no STOP condition triggered.** Max abs score drift: 0.00000054 / 0.0000727 / 0.0000181 (observation only, consistent with D2's own tiny forward-pass diff). Gates: `cargo check` clean both configs; `cargo test` feature-off 46→47 (+1, `nfc_completeness_guard`, unconditional); `cargo test --features fa-inference` with `ORT_DYLIB_PATH` set + `FA_REQUIRE_ORT=1` 66→70 (+4: +1 NFC guard, +3 `e2e_parity`), **0 skips** (confirmed via `--nocapture` grep); same command with `ORT_DYLIB_PATH` unset + `FA_REQUIRE_ORT` unset → 70 passed, 6 clean skips (3 `onnx_fixture_parity` + 3 `e2e_parity`); same with `ORT_DYLIB_PATH` unset + `FA_REQUIRE_ORT=1` → 6 hard failures, 64 passed, confirming skips cannot be forced quiet; `npm run lint` clean (after fixing 2 strict-null TS errors in the new generator script); `npm test` 75 files/1894 passed/1 skipped (unchanged — no `src/` production file touched); golden replay 6/6 (unchanged). `faTextNormalize.ts`/`textNormalize.ts`/`canonicalize()` diff: empty (confirmed via `git diff --stat`). No new crate added (`Cargo.toml`/`Cargo.lock` diff: empty). No frontend/IPC wiring, no `FaEvent` emission, no capability gate — unchanged from D2/D3's own scope boundary. Remaining for this task: multi-segment span attribution, session/model caching, and all frontend/IPC wiring (still blocked on the open capability-gate decision, untouched by this slice). **Slice D5 (2026-08-12): closes the text->spans seam and extends parity coverage to all 5 languages (en/es/fr/de/pt).** Full seam-rule writeup and findings: `docs/ws1-sync-pipeline/fa-text-to-spans-seam-d5-2026-08-12.md`. (a) Investigated D4's stated gap ("no test takes raw text through to spans") and found it **already closed at e1f6e57** for en/es — `fa_onnx.rs::e2e_parity::run_one` already derives `target_token_ids` from the fixture's raw `_provenance.text` via production `text_to_token_ids()` (which itself calls `fa::text::normalize_for_forced_alignment`), asserts it against the fixture's recorded sequence, then uses the **derived** ids (not the fixture's) for the forward pass/Viterbi/merge; verified by rerunning it live. (b) Non-vacuity re-proven: incremented `target_token_ids[0]` in the es fixture by 1, reran `e2e_es_resultan_inutiles` — failed at the `assert_eq!` exactly as expected; restored via `git checkout --`, reran — passed. (c) Regenerated all 3 existing e2e fixtures via `capture-fa-e2e-reference.py` — `expected_spans`/`target_token_ids`/`blank_id`/`input_samples` all byte-identical to e1f6e57 (direct JSON comparison); only diff is a new `_provenance.targetTokenIdsRole` note clarifying the field's dual role (expected tokenizer output, also required as `forced_align`'s target-sequence input — an unavoidable CTC-DP contract). (d) **fr/de/pt: hit STOP condition (g) (no real fr/de/pt audio anywhere in the repo or private corpus — checked `~/Downloads/All Projects Test Data/` directly, and the only fr/de/pt material was Common Voice text-only sentences plus one English clip reused structurally for ONNX export fidelity), reported to owner, owner approved sourcing from a public corpus.** Downloaded 3 short real utterances from `google/fleurs` (CC-BY-4.0, HF-hosted, validation split) via `curl` against Hugging Face's presigned dataset-server URLs, into `.work-phase4/replay/{fr,de,pt}/audio_16k.wav` (gitignored, matching the existing en/es pattern) — confirmed 16kHz mono float32 before use, exact files/sizes/text confirmed with owner before downloading. `capture-fa-e2e-reference.py` gained a `RAW_CLIP_CASES` path (no D2 onnx-emission fixture exists for these languages to reuse) that runs the ONNX forward pass in-process via `onnxruntime` directly against each whole clip, then feeds it into the same independent `torchaudio.functional.forced_align`/`merge_tokens` step the 3 D2-sourced cases use — deliberately does NOT commit a separate emission-matrix intermediate (an early attempt produced a 2.59 MB single fixture, over budget; reverted). `generate-fa-e2e-tokens.ts`'s `CASES` and `fa_onnx.rs`'s `e2e_parity::FIXTURES` each gained the matching 3 entries (no new logic — both paths already generalize over language) plus 3 new `#[test]` fns. **Result: zero divergences on all three, same zero-tolerance gate as en/es** — fr-pas-juste (65 target tokens, 230 frames, 65 merged spans, max abs score diff 0.0000126), de-nicht-fair (61/269/61, 0.0000161), pt-site-publico (70/281/70, 0.0000017); zero target-token-sequence mismatches, zero unrepresentable/dropped words in the 3 chosen sentences. Fixture sizes: fr 1.63 MB / de 1.88 MB / pt 1.84 MB (all under the 2 MB cap); cumulative FA fixture footprint 12 MB (under 20 MB budget). Gates (all green): `cargo check` clean both configs; `cargo test` feature-off 47 (unchanged); `cargo test --features fa-inference` ORT set + `FA_REQUIRE_ORT=1` → 70→73 passed (+3), 0 skipped; ORT unset + `FA_REQUIRE_ORT` unset → 73 passed, 9 clean internal skips (3 `onnx_fixture_parity` + 6 `e2e_parity`); ORT unset + `FA_REQUIRE_ORT=1` → 64 passed/9 hard failures (skip-forcing still impossible); `npm run lint` clean; `npm test` 75 files/1894 passed/1 skipped (unchanged — no `src/` production file touched); golden replay (`phase4-handoff-replay-sync.test.ts`) 6/6 (unchanged). `faTextNormalize.ts`/`textNormalize.ts`/`canonicalize()` diff: empty (confirmed via `git diff --stat`). No new crate. **Slice D6 (2026-08-12): frame->time conversion, plus closing three verification gaps (frame->time absent, `EmptyTokenization` unverified, missing-dylib typed error unverified) and the live TS/Rust `FaErrorKind` drift.** (a) Frame->time: new `fa_onnx.rs::frame_to_seconds`/`token_span_seconds`, stride = 320 samples (16kHz) — the PRODUCT of the shared Wav2Vec2 feature encoder's `conv_stride: [5,2,2,2,2,2,2]`, read directly from each of the five jonatasgrosman languages' own HF `config.json` (`.work-phase4/spike-runtime/models/*/config.json`, gitignored, confirmed byte-identical across all five before writing the constant) — deliberately NOT the `fa-emission-*.json` fixtures' empirically-measured `frame_rate_fps` (a different model family, MMS_FA, and downstream of stride rather than an independent measurement — its own per-clip drift, 49.615–49.841, is why). `capture-fa-e2e-reference.py` gained a mirroring `frame_to_seconds` (same 320/16000 formula, same op order) and now writes `start_seconds`/`end_seconds` into every fixture's `expected_spans`; all 6 `fa-e2e-alignment-*.json` fixtures regenerated — every pre-existing field (`input_samples`/`target_token_ids`/`blank_id`/span `token`/`start`/`end`/`score`) confirmed byte-identical to `c7834cd` by direct field comparison, only the two new per-span keys added. New `e2e_parity` seconds assertion (all 6 fixtures) at a `1e-9`s tolerance — justified as ~6 orders of magnitude above double-precision's own ~1e-15s rounding floor at this magnitude, not chosen to make the test pass — observed max abs deviation was `4.44e-16` (a single ULP), confirming both sides compute the identical formula. Non-vacuity: perturbed `en-deep-night`'s span[0] `start_seconds` by +0.5s, reran — failed with the expected diverges-not-tolerance message; restored, reran — passed, deviation back to `4.44e-16`. (b) `EmptyTokenization`: `align()` split into a thin `AppHandle`-resolving wrapper plus a new `align_with_model_path(model_path, audio_path, segments, language)` (no behavior change) so a test can drive the real pipeline without a live `AppHandle` (nothing in this suite constructs one, same rationale as `onnx_fixture_parity`/`e2e_parity`'s own `fa_models_dir()`). New `empty_tokenization` test reconstructs a short real WAV from the D2 `en-deep-night` fixture's own `input_samples` and passes real Japanese place-name text (`東京 大阪` — every word absent from the ASCII-only `en` vocab, verified by inspection), asserting `Err(FaOnnxError::EmptyTokenization)` specifically; non-vacuity confirmed by swapping in `"hello world"` and observing the expected failure (11 spans, wrong variant), then reverting. (c) Missing dylib: new `missing_dylib` test calls `run_forward_pass` directly with `ORT_DYLIB_PATH` genuinely removed, asserting `Err(FaOnnxError::OrtInit(_))` as the outcome, never gated by `ort_dylib_or_skip`. Determinism under `cargo test`'s default parallelism (no `--test-threads=1`, no new crate): a new `require_ort::ORT_ENV_LOCK` `Mutex<()>` serializes every `run_forward_pass` call site in the crate (via a new `with_ort_env_lock` wrapper — real lock in test builds, no-op passthrough otherwise) AND `ort_dylib_or_skip`'s own presence check — the first implementation locked only the call sites and missed the check, which under `FA_REQUIRE_ORT=1` has a PANIC branch; caught this for real by running the full suite concurrently (not by reasoning alone) — `onnx_fixture_parity::parity_en_deep_night` failed once with the exact race predicted, fixed, then reran the full ORT-set+`FA_REQUIRE_ORT=1` suite 6 consecutive times clean before trusting it. (d) `FaErrorKind` drift: `faBoundaryTypes.ts`'s union was missing `'inferenceFailed'` (live since D2's `FaError::inference_failed`, `fa.rs`) — added, with a doc comment pointing at the new Rust guard. New `fa.rs::every_fa_error_kind_variant_serializes_to_its_expected_camelcase_string` test uses a wildcard-free exhaustive match (compiles only if every variant is listed) plus per-variant `serde_json` string assertions — catches a future Rust variant added without its TS counterpart, but is explicitly one-directional (documented in both files): cannot catch a TS-side rename/addition drifting away from Rust. **Doc-omission finding (recorded, not fixed per this slice's scope): `fa.rs`'s own module-level doc comment (top of file, "There is NO model and NO inference here — `fa_align` always returns a typed not-implemented error today") has been stale since D2 wired real `fa-inference`-gated inference — it was never updated across D2–D5 and still unconditionally asserts the pre-D2 boundary-skeleton claim.** Gates (all green): `cargo check` clean both configs; `cargo test` feature-off 47→48 (+1, the `FaErrorKind` guard, unconditional); `cargo test --features fa-inference` ORT set+`FA_REQUIRE_ORT=1` 73→76 (+3: `empty_tokenization`, `missing_dylib`, `FaErrorKind` guard), 0 skips, confirmed clean across 6 repeated runs; ORT unset+`FA_REQUIRE_ORT` unset → 76 passed, 10 clean skips (3 `onnx_fixture_parity` + 6 `e2e_parity` + 1 `empty_tokenization`; `missing_dylib` never gated); ORT unset+`FA_REQUIRE_ORT=1` → 66 passed/10 hard failures; ORT set+`FA_REQUIRE_ORT` unset → 76 passed, all real. `npm run lint` clean; `npm test` 75 files/1894 passed/1 skipped (unchanged — no `src/` production file touched besides the type-only `faBoundaryTypes.ts`); golden replay 6/6 (unchanged). `faAnchors.ts`/`faTextNormalize.ts`/`textNormalize.ts` diff: empty. `Cargo.lock` diff: empty (no new crate). Fixture sizes: all 6 `fa-e2e-alignment-*.json` under 2 MB (largest, de, 1.88 MB); cumulative `fa-*.json` footprint 12 MB (under the 20 MB budget). No windowing/chunking/stitching/cancellation/span-consumption/asset-resolution work — out of scope, deferred to D7 per the slice's own boundary.
- [ ] 6. Resume Pipeline Contract Program — paused, blocked on task 4 (`docs/ws1-sync-pipeline/sync-pipeline-v2-plan.md` Part J)
- [ ] 7. Re-attempt the boundary-quality watcher — prior attempt reverted (safety-bound failure, React render loop, uncalibrated formula); resumption point at `docs/ws1-sync-pipeline/watcher-revert-2026-08-03.diff`, not source to reintroduce as-is
- [x] 8. K13 fix — lock preservation across resync. **Done 2026-08-11.** Owner ruling R-C: fixed fresh against `main`, porting only the logic/idea from the parked `model-p-editor-work` branch, never its stale code — `preserveSegmentLocks` (`App.tsx`) restores `locked`/`startTime`/`duration` by unique `assetId` after `autoMatchSegments`/`preserveEffectFields`, bound-checked and validated against `findPartitionViolations`. Verified: 11 unit tests (`src/services/preserveSegmentLocks.test.ts`), the inverted `scripts/phase4-step-w-k13-repro.test.ts` (now proves the fix holds, 3/3), 5 manual tests. Gates at close-out: `npm run lint` clean, `npm test` 72 files/1803 passed/1 skipped/0 failed, golden replay 3/3, `cargo check` clean.
- [ ] 9. Phase 3b — language-keyed number/currency normalization (per-language number words and reading rules, currency equivalents, inverted thousands separators, French elision vs. English contraction expansion; full spec `docs/ws1-sync-pipeline/sync-pipeline-v2-plan.md` Part H.5). GATE: English path must stay byte-identical to today's baseline.
- [ ] 10. Phase 3c — hyphen/word-split fix (`textNormalize.ts` glues a mid-call hyphenated word into one alignment word while Whisper emits two tokens, understating the segment's end). **Must land before Phase 5; gates the Stage 1 lock** — the last Stage-1 index-shifting event (`docs/ws1-sync-pipeline/sync-pipeline-v2-plan.md:3725-3726`).

**Rulings (WS1-specific):**
- **R-A — 22 blank `boundary-quality-flag` rows deferred, non-blocking** (2026-08-11). WS1 does not pause for an ear-listening pass to fill them in. Item tracked in Deferred / Known Bugs below.
- **R-B — the fr/de/pt "unvalidated language" warning (Step T.7) ships with Phase 3/task 5**, same task/release, not after. Referenced in task 5 above.
- **R-C — `model-p-editor-work` stays permanently unmerged; K13 fixed fresh against `main`, porting only the logic/idea, never the stale branch code** (2026-08-11). **CLOSED** — full fix record at task 8 above.
- **Heading-wildcard assignment (Option A)** — unscripted audio (spoken chapter headings) is absorbed entirely by the preceding segment, logged as an explicit `unscripted-gap` entry. Owner decision 8, recorded in `docs/ws1-sync-pipeline/sync-pipeline-v2-plan.md`'s Steps Y–Z section. Blocks v2 Phase 5, not Phase 3.
- **Spanish forced-alignment gate — cleared.** Step F's breath-aware reference scores p95 50.4ms against the approved 250ms gate (1 of 22 pauses over) — passes. All three WS1 Rust-integration gates (Spanish accuracy, structural checks, heading assignment) are closed. Evidence: `docs/ws1-sync-pipeline/spanish-gate-scoring.md`, `docs/ws1-sync-pipeline/measurements/`, `docs/ws1-sync-pipeline/roadmap-2026-08-07.md` §D2, `docs/ws1-sync-pipeline/sync-pipeline-v2-plan.md` Step U/T.7.
- **ort/onnxruntime native-runtime blocker — RESOLVED, not yet ratified into `project-state.md`** (2026-08-12). R-M's "from-source onnxruntime build required" premise superseded: `ort`'s `api-NN` Cargo features are a configurable minimum-version floor (default requires onnxruntime ≥1.27; disabling all `api-NN` features drops the floor to 17), and the onnxruntime-osx-x86_64 binary (v1.23.2 is the correct pin, not v1.22.0) loads and runs a full forward pass through the real Rust `ort` binding with fidelity-verified output. Full investigation evidence: `docs/ws1-sync-pipeline/measurements/runtime-unblock-2026-08-12.md`. **Update, Slice D2 (2026-08-12): this is no longer scratch-only** — `src-tauri/Cargo.toml` now carries `ort` as a real (OFF-by-default, `fa-inference`-feature-gated) optional dependency, and `src-tauri/src/fa_onnx.rs` wires a real forward pass into `fa_align`; see task 5 above for the full slice record. **The ruling update to R-M and the still-open R-N packaging decision remain formal `project-state.md` edits — deferred to the end-of-Task-5 documentation pass, pending owner approval**, per the process rule below.
- **Process rule (permanent, all future WS1 tasks):** during implementation, only this file and files under `docs/ws1-sync-pipeline/` are written. `CLAUDE.md`, `docs/history.md`, and `project-state.md` require explicit owner approval and are batched to task completion — never touched at the end of a part or sub-task without that approval.

Full record for all five: `docs/history.md`'s "Decisions Log — Dissolved from `docs/decisions/`" section.

**Deferred / Known Bugs:**
- **CLOSED 2026-08-05 — Adaptive per-voice silence thresholds (Phase 3d).** Investigated and SKIPPED per Phase 2b's own measurement: the fixed −45dB threshold is not the binding constraint (ground-truth pauses verified real against the waveform; the failure is entirely token-side, not silence-side). Reopens only if task 5/Phase 3's post-forced-alignment measurement shows a silence-side cost. Evidence: `docs/history.md:4265`, `docs/ws1-sync-pipeline/ws1-master-roadmap.md:118,325`, `docs/ws1-sync-pipeline/sync-pipeline-v2-plan.md:553`.
- **`boundaryUsedFallback` calls `isBreathSilence` with 4 arguments instead of 5** (`src/services/snapBoundaries.ts`) — the omitted 5th parameter silently defaults to disabling the seam exemption, so every `validateBoundaryQuality` reading on a seam-exempted pair has been wrong since it shipped. Diagnostic-only — never affects a committed boundary. Scheduled for v2 Phase 7; self-resolves if v2 Phase 6 deletes the exemption instead.
- **Stuck `resizingId` on an early-bail drag start** (`src/services/dragSession.ts`) — **WONTFIX (owner ruling, 2026-08-08).** Unreachable through the UI (both bail conditions require state a real gesture can't produce), and fixing it would retire a characterization test that proves the WS2 task-1 extraction was behavior-preserving. The desired behavior is pinned as a deliberately skipped spec test (`dragSessionHarness.test.ts` PART 4) and the residue is actively audited by `acknowledgeKnownResidue`, which fails if the residue ever stops appearing. Ruling lapses if the path ever becomes reachable.
- **22 blank `boundary-quality-flag` rows** in `scripts/fixtures/verification-baseline.csv` — deferred, non-blocking (owner ruling R-A, 2026-08-11, see Rulings above). WS1 does not pause for an ear-listening pass to fill them in.

Full state: [`docs/ws1-sync-pipeline/`](ws1-sync-pipeline/).
