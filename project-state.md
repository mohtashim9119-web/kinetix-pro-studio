# Kinetix Pro Studio — Project State

> **Purpose:** Living source of truth for project status. Updated at the end of every work session.
> Distinct from `CLAUDE.md` — that file covers architecture/conventions; this file tracks where we are.

---

## Current State

| Field | Value |
|---|---|
| Last updated | 2026-08-05 |
| Current HEAD | `124ad3d` ("docs: close Phase 0 — lock defect recorded, key disambiguation, backup identity fix, results") on branch `webgl2-effects-engine` — **committed, not pushed**. `tsc --noEmit` clean, `vitest` **1281/1281** — the drop from the previously-recorded 1284 is Phase 1's deletion of 14 tests exercising the now-deleted duplicated gap-fill in `alignScenestoTranscript` (`docs/sync-pipeline-v2-plan.md`'s Phase 1 — corrected 2026-08-04: 29 was a pre-deletion estimate, not a verified count), **not a regression**. |
| App status | Shipping desktop app — Tauri DMG/installer, native ffmpeg sidecar export. No server, no web hosting. |
| Target users | YouTube creators — initial internal use across 5–10 channels |
| Repo | TBD |
| Restore tag | `sync-known-good-2026-06-20` → commit `bab79b0` — historical pre-sync-rewrite baseline. `sync-known-good-2026-07-29` → commit `bd9e919` — current active bisect target. See Key Invariants (a). |

Sync system rewrite closed (WS1a→WS6 + token-stealing fix); see `docs/history.md` for the full record. Export UX (live timer + completion chime) and the heading text quality fix (1080-reference scale + supersampled rasterization) have also shipped since. Most recently (2026-08-03): the index-based seam exemption fix for `isBreathSilence`'s multi-fragment override — ear-verified 86.8% → 96.2% correct cuts on the V6 production project — see `docs/boundary-drift-investigation.md` and Active Tasks below for the still-open word-shift defect it surfaced. Before that (2026-07-31): the window-overlap silence-candidacy regression fix (a real production bisect found the intra-segment SPAN/tolerance test in `isBoundarySilenceCandidate` was rejecting genuine boundary silences wholesale — deleted, not re-tuned), the resulting contiguity invariant fix in `snapCoveredBoundaries`, the Timeline absolute-positioning + lane redesign + cross-lane boundary markers, segment-1 head-extension (`headExtendFirstSegment`), the Bug C consecutive-run survival gates (recovered from a dropped stash and landed), the sync-log copy button, and Segments-panel spacing/hover-affordance polish — see `docs/history.md` → "Window-Overlap Silence Regression Fix, Timeline Redesign, Bug C Landed, Head-Extension, Copy-Logs, Panel Polish (2026-07-31)". All foundational/export/desktop work remains shipped and stable, including the clean-slate re-sync Architecture Shift (closed 2026-06-24) and the Effects Tab Rebuild (transitions 10/10, clip effects 7/7). One sync item remains open — the word-shift defect, see Active Tasks.

The v2 sync-pipeline architecture plan (`docs/sync-pipeline-v2-plan.md`) is **accepted** (2026-08-03) as the path forward for the word-shift defect, and was revised the same day (**Revision 2**): stage contracts (Part J), per-stage lock gates (Part D), a blocking Stage 1 Transcript Inspector (Phase 1b), the Russian descope (supported languages now EN/ES/FR/PT/DE), the real-test-data corpus inventory (Part D.0 — all seven corpus projects are English; the Phase 0 backups for V6 and the 173-project already exist in `All Projects Test Data/Projects Backend Data/`), and an adversarial self-audit (Part K — notably: old Phase 8's Stage-1 items moved into Stage 1; `applyAnchorBasedTiming` has a lock-toggle UI consumer at `App.tsx:1686` that Phase 4 must preserve; **K13**, added 2026-08-04, records that lock preservation across resync is currently broken — see Deferred Known Bugs below). **Phases 0, 1, and 1b are now CLOSED** (owner-verified 2026-08-04; commits `0f949d5`, `7e783a3`, `42f0377`, `124ad3d`) — results recorded in the plan's Phase 0/1/1b entries.

**Phase 2a (model swap) — CODE COMPLETE, verdicts pending (updated 2026-08-05).** The Spanish-or-French corpus blocker is resolved (a `Spanish Project` folder now exists in the corpus directory with audio/script/scene-doc) and `ggml-large-v3-turbo.bin` is provisioned in `src-tauri/models/` (measured: 1,624,555,275 bytes on disk, ~2.1-2.2 GiB peak/resident during inference — H.9). `whisper.rs` now targets the turbo model with `-l auto`/stored-language, `--dtw base.en` dropped (was a no-op, model-specific preset name), `-np` dropped (stderr is now scanned for the auto-detected-language line, verified not to change stdout token output). `Project.language` (persisted, detected-once-then-sticky, user-overridable in Project Settings) and the H.4 `unsupported-language` guard (error-severity log entry + persistent dismissible banner) both ship. Determinism re-verified on turbo (PASS, both with the literal Phase 0 command and with the actual shipped `-l auto` flag — byte-identical MD5s across all four runs on a small English corpus file). `tsc`/`cargo check`/`vitest` (1281/1281) all clean; the Language settings UI and the full unsupported-language guard path (log entry + badge + banner) were smoke-tested live in the dev server.

**Step 5's real-corpus half is now done (2026-08-04/05): fresh turbo resync + `window.__transcriptInspector()` CSV pull for V6, 173-seg, and the Spanish project** (`docs/V6-Smear-Phase2a.csv`, `docs/173-Smear-Phase2a.csv`, `docs/Spanish-Smear-Phase2a.csv`). Analysis of those CSVs against the base.en baseline (`docs/sync-pipeline-v2-plan.md`'s Phase 2a entry has full detail) found:
- **A genuine ~9.7s content dropout on V6** (78.97–88.67s): turbo's transcript is entirely missing segments 27–29's text ("But something stayed in you. Small and permanent. A new understanding of what the night actually is."), which base.en captured cleanly. Confirmed by full-transcript search (turbo has zero occurrences of "stayed"/"permanent" anywhere). This is a model-swap ACCURACY regression, not an alignment failure — no boundary or timing fix can recover text the model never emitted. **Consequence: Phase 2b must measure whether large-v3 (non-turbo) also drops this passage — it is now a real accuracy candidate, not merely a reference ceiling.**
- **The 173-project's segment 1 (title card) and segment 13 (planted "blue monkey" string) are confirmed correct skips** — neither's text appears anywhere in the turbo transcript, same as they didn't in base.en.
- **173-project segment 112 ("Some don't emerge.") is a turbo-era regression** (this exact boundary was a verified-correct `control` at Phase 0 on base.en). Mechanism: turbo drops the word "Some" (base.en has it, turbo doesn't) on top of a pre-existing, model-independent normalization asymmetry (script-side "don't" expands to "do not" via `textNormalize.ts`'s whole-string contraction step; transcript-side Whisper always splits it into two sub-word tokens `don`+`'t`, which never re-triggers that expansion, in either model era) — with "Some" present, base.en's hole-tolerant run computation cleared the run-survival gate; without it, turbo's match (1 of 4 normalized words, longest run 1) most likely fails it. Presented as the most likely mechanism from token evidence, not verified by running the pipeline.
- **A recurring small-scale word-drop pattern beyond the single large V6 case** — turbo silently drops short words ("Three", "No", "afraid", "Fen's", part of "Thick", part of "Catachan") at several other boundaries in the cross-era diff (Step 3, plan doc). Read as a systemic accuracy trait on this corpus, not isolated incidents.
- **Turbo-era smear baseline recorded** (V6/173/Spanish, mirroring Phase 1b's base.en table) — see the plan doc's Phase 2a entry. Notably 173's negative-smear fraction improves (23.6%→15.4%) while V6's is roughly flat (34.4%→33.6%) — not evidence the timing source improved; Phase 2b measures that properly.
- **`verification-baseline.csv` now carries 69 blank `phase-2a` verdict rows** (47 existing boundaries + 22 new sync-log-flagged candidates, cross-referenced word-text-keyed per K9) for the owner's ear. **Phase 0's correct-verdict count is 30 of 47 — phase-2a's gate is ≥30 correct once the owner fills the cells.**
- **The 173-project's scene doc has drifted to 175 bracket-tagged segments** (was 173 at Phase 0) — flagged, not resolved; re-verify the count before trusting any future "173-project, segment N" reference by position.

**Phase 2a — DONE (2026-08-05).** The owner's ear-listening pass over the original 47-boundary verification set landed: **38 correct, 5 word-shifted, 1 FAIL, 2 N/A (transcription loss, not a boundary failure), 1 unverified.** Against the 44 rows with a real placement verdict: **38/44 verified — Phase 0 30/47 → phase-2a 38/44, gate ("does not get worse than Phase 0") PASSED** (39/44 if the owner reclassifies `seasons than you || can count and` as correct rather than word-shifted; doesn't change the outcome). Net improvement: 9 of the original 11 ear-verified word-shift cases resolved (2 remain: `does the same || what the job`, `s youngest scout || a girl of`); all 5 of Part L's short-segment-run cases resolved. Three findings recorded (plan doc's Phase 2a Step 7 has full detail, no code changed): (1) a new third failure class — `seasons than you || can count and` cuts cleanly at a real pause, but the narrator's pause disagrees with the script's segmentation; a timing-source upgrade will NOT fix it, added to Part L; (2) pairIdx-20 ("chitin thick enough") now places correctly in live production, but the pinned `syncTiming.test.ts` fixture is synthetic/frozen and still correctly locks the still-present code defect — fixture NOT updated, Phase 6b's scope does NOT shrink; (3) R10 — 2 seam-related regressions (`s youngest scout || a girl of` V6, and a newly-found `They're the worst` 173-seg case) plus 2 control regressions (`and fat-soaked moss || bound with sinew`, `when you report || but your hands`), below the systemic threshold, recheck-and-recorded per Part F, **no constant retuned**. A missing regression row (`They're the worst`, 173-seg segment 5-6 — never in the original 47, missed at Phase 0) was added as a phase-2a-only row, not backfilled to Phase 0.

**Spanish (Step 5) — run completed cleanly, boundary quality UNVERIFIED, written acceptance recorded.** 27 scene-doc segments, genuine Spanish narration confirmed; the turbo transcription completed with no abort/error (399 raw tokens, 36 dropped, 363 kept, 27 silences, 92.02s audio). No Spanish boundaries have been listened to or added to `verification-baseline.csv` — that requires a Spanish speaker's ear, which this session does not have. Accepted in writing at the plan's Stage 1 lock-gate entry, with an explicit reopening trigger (voided the moment any Spanish-specific normalization/alignment code ships in Phase 3b). French/Portuguese/German corpus material remains completely absent — same accepted-gap treatment, per H.8's dormant-rules allowance.

**Stage 1 LOCK: NOT PASSED**, blocking list recorded in the plan's Stage 1 lock gate section: (a) smear thresholds unmet by 24-34× → needs Phase 3; (b) non-English corpus — **PARTIALLY RESOLVED**, Spanish exists and runs cleanly but is unlistened (accepted in writing, reopens at Phase 3b); French/Portuguese/German still absent (accepted); (c) ~~3 short-segment-run boundaries not yet in the verification set~~ **RESOLVED 2026-08-04** — 5 added, satisfying Part L's ≥3 requirement; (d) Contract IN/1→2 not yet verified guarantee-by-guarantee; (e) cross-cutting regression checklist (D.-1) not yet run; (f) ~~Phase 2a's own 69-row verdict gate unfilled~~ **RESOLVED 2026-08-05** — owner's listening pass landed, gate passed (38/44, see Phase 2a entry above). **Next actions:** Phase 2b (read-only, measurement-exempt) or Phase 3, per priority — items (a)/(d)/(e) still block the Stage 1 lock itself. Do not begin Phase 2b/3 without a fresh session; this session's work stops at Phase 2a's close.

---

## Active Tasks

- **Boundary-placement fix — SHIPPED, ear-verified (2026-08-03).** `isBreathSilence`'s multi-fragment override now classifies breath-vs-boundary by token INDEX rather than token TIMESTAMP (Whisper timestamps blur 100-900ms across a real seam; indices, from the Hirschberg alignment pass, don't). Fixes 8 real boundaries on the 447-segment V6 production project (segs 34, 96, 162, 316, 338, 352, 405, 412). User resynced V6 with the fix in the tree, scrolled the full timeline, and confirmed cuts now sit in pauses: **86.8% → 96.2% correct cuts**. Full evidence, dead ends, and tooling notes: `docs/boundary-drift-investigation.md`. Wired NEXT-side only — the CURR-side variant is permanently disabled (confirmed unsound on a second, independent 173-segment project). See `snapBoundaries.ts`'s own doc comment and `CLAUDE.md`'s entry for the mechanism.
- **Word-shift defect (OPEN, narrowed by Phase 2a)** — a segment's cut point lands one or more words off from where the sentence actually breaks. **Originally 11 ear-verified cases** (segments 28-29, 60-61, 77-81, 105-106, 117-118, 130-131, 144-147, 188-189, 222-223, 295-296, 428-429, V6 project); **as of the Phase 2a model swap (2026-08-05), only 3 remain word-shifted:** `does the same || what the job`, `s youngest scout || a girl of` (both still failing after the swap), plus `seasons than you || can count and` — this last one is a **newly identified third failure class**, distinct from both mechanisms below: the cut is clean and lands at a genuine acoustic pause, but the narrator's pause disagrees with the script's own sentence break, so a timing-source upgrade will NOT fix it (see Part L amendment, plan doc's Phase 2a Step 7). **Two known causes remain live** (`docs/sync-pipeline-v2-plan.md`'s Part L): (1) **picker over-reach** — the boundary picker widens its silence-candidate search window when the claimed inter-segment gap looks narrow (0.244s mean on defective pairs vs. 0.700s mean on clean pairs), fixable by v2 Phase 5's fence; (2) **token-smear cascade** — a multi-segment cascade caused by Whisper timestamps landing inside the preceding silence, which no boundary rule can repair — only **v2 Phase 3** (timing-source upgrade) fixes this class. Phase 2a also surfaced 2 new phase-2a-only control regressions (`and fat-soaked moss || bound with sinew`, `when you report || but your hands`) and a previously-missed seam-exemption case (`They're the worst`, 173-seg) — recheck-and-recorded under R10, not retuned, below the systemic-regression threshold. The aligner's SPANS remain exonerated (all 447 confirmed correct) but its TIMESTAMPS are not — see the correction added to `docs/boundary-drift-investigation.md`. Two candidate fixes tried pre-v2 and **both failed**: FENCE (window-fencing) directly conflicts with the 8 Candidate-1 breath-exemption fixes above; QUIET (quietest-point bias) fails 3 of 4 hard correctness checks at every window size tested. **Fix plan accepted 2026-08-03, revised 2026-08-04 (Part L), Phase 2a closed 2026-08-05: `docs/sync-pipeline-v2-plan.md`.** Full detail: `docs/boundary-drift-investigation.md`.
- **Pipeline Contract Program — stays paused.** Was already paused pending the (now-shipped) boundary-quality checker Phase 1; now additionally pending the word-shift fix above. Pair 1's own R2/floor-clamp analysis is superseded by the boundary-drift investigation — see `docs/sync-pipeline-contract-plan.md`'s re-stamped header. Two new, uncalibrated validators shipped alongside the boundary fix outside the formal contract-program sequence (same "ships when ready, doesn't wait for its formal turn" precedent as the boundary-quality checker itself): a Contract 3→4 `low-word-coverage` checker (`validateWordCoverage`, `syncContracts.ts`) and grouped sync-log entries (`buildGroupedViolationEntry`, `syncLog.ts`) — both **UNVERIFIED**, no manual confirmation yet in the running app.
- **Phase 2 boundary-quality watcher — REVERTED 2026-08-03** (no change from prior status). An uncommitted implementation (`applyBoundaryWatcher` + `verifyCommittedBoundaries` auto-correction, `boundaryQuality.ts`) was built and tested against production-verified projects, then reverted before commit for three reasons found during that verification: (1) a safety-bound failure on a real production project — a correction produced a degenerate near-zero/negative segment duration and a visible black gap; (2) a React render loop triggered by the new `setProject` write path in the Apply-Sync effect; (3) `findSilenceRegionCenter`'s region-centering formula (the `quietestAmplitude * 2` interim threshold) was never calibrated against real audio. The full diff is preserved at `docs/watcher-revert-2026-08-03.diff` — starting point for any future attempt.
- **Short-segment token-smear cascade (NEW finding, 2026-08-04)** — see the word-shift entry above and `docs/sync-pipeline-v2-plan.md`'s **Part L**: dense runs of sub-1s, 1-3-word segments are structurally vulnerable to Whisper's timestamp smear, since defect severity ≈ smear/segment-duration. Blocks Stage 1 lock (verification set needs ≥3 short-segment-run boundaries, not yet added — owner listening task).
- **(b) Stage 2 adaptive per-voice silence thresholds** — noise-floor estimation, falling back to the current fixed -45dB when estimation is unreliable.
- **(c) Sync loading screen redesign** — live 0–100% progress (currently a static "Preparing your project…" message, `SyncLoadingOverlay.tsx`).
- **Working rule:** audit/investigation reports must be persisted into `docs/` (not left to live only in chat transcripts) — implementation work must never depend on recalling a prior conversation.

## Deferred Polish Features

- **Export quality pass — real color-space conversion + cross-segment drift correction** (was "Phase C" in the now-deleted `docs/webcodecs-architecture-plan.md`). The shipped WebCodecs + WebGL2 Worker Export path only tags bt709 color space at MUX time (`muxOnly.ts`) rather than performing true color-space conversion, and has no dedicated frame-timing/drift correction against the audio master clock beyond what the shared compositor already provides. Deliberately deferred, not a known defect — mux-time tagging measurably fixed the preview-vs-export color mismatch, and no drift complaint has been reported. Revisit only if a real color-accuracy or drift issue surfaces.
- Version snapshots (2 open design decisions before building: asset-restoration Design A vs B, and full-rewind-on-restore)
- Auto-captions (reuse Whisper transcript tokens as a timed text layer)
- Multi-user support — team accounts vs. staying single-user is still an open call; revisit if/when multi-user demand materializes
- Multi-language support — bundled model is English-only (`ggml-base.en.bin`); whisper.cpp silently ignores `-l auto` on `.en` models. Requires bundling `ggml-base.bin` (~148MB, multilingual). See `docs/history.md` → "Sync System Rewrite (2026-07-24 to 2026-07-29) — Archived".

## Deferred Known Bugs

- **Lock preservation is broken across resync — positions reset AND lock flag cleared (found 2026-08-04, Phase 0 closure).** Owner's manual test on the 173-seg project: moved segment 27 to overlap segment 28, locked BOTH, ran Apply Sync. Both segments reset to their original unlocked positions and the `locked` flag itself was cleared on both. This CONTRADICTS `docs/sync-pipeline-contract-plan.md:359`'s stated invariant "Locks are authoritative everywhere" (which lists five enforcement sites: `distributeSegmentTimes`, `applyAnchorBasedTiming`, `snapCoveredBoundaries`, `headExtendFirstSegment`, both gap-fill passes) — that line has been corrected in place to note the guarantee only holds once a segment carries `locked: true` into those five sites, which currently never happens. Root cause (read-only investigation, hypothesis confirmed): clean-slate resync mints every segment fresh via `parseProjectData` (`App.tsx:318`) from a `RawSegment` (`App.tsx:161-172`) that has no `locked` field, then builds the final `VideoSegment` via `{ ...s, id: crypto.randomUUID(), ... }` (`App.tsx:513-528`) without ever reading `project.segments`. Same clean-slate discipline as `anchorStart`/`anchorSource` (the old stableKey merge loop was deleted in step 3a, commit `452e1eb`) — except no equivalent carry-forward was ever built for `locked`, and fresh per-run `crypto.randomUUID()` ids make an id-keyed restore impossible regardless. The five sites that *read* `locked` (`syncEngine.ts:217,232,300`; `snapBoundaries.ts:697,899`; `whisperService.ts:1364,1369,1488`; `syncContracts.ts:295`) are all checking a flag that is structurally always `undefined` by the time Apply Sync reaches them. Test case now tracked in `docs/verification-baseline.csv`'s locked-segment row (case_type `locked-segment`, verdict FAIL). Fix path: `docs/sync-pipeline-v2-plan.md`'s **K13** — Stage 3 must BUILD an order-keyed carry-forward of `locked` from the pre-sync segments into the freshly parsed array, verified by a dedicated Stage 3 lock-gate test using this exact repro. Not yet fixed — this file's fix is documentation-only (Phase 0 closure); no code under `src/` was touched.
- **`boundaryUsedFallback` calls `isBreathSilence` with 4 arguments instead of 5** (`src/services/snapBoundaries.ts:381-382`) — both the curr-side and next-side calls omit the 5th `otherSideLastTokenIdx` parameter, so it silently defaults to `-1` (the value that disables the seam exemption entirely, per the parameter's own doc comment at `snapBoundaries.ts:570`). The next-side call should pass curr's own `lastTokenIdx` (real seam evidence), the same way `snapCoveredBoundaries` and `alignScenestoTranscript`'s gap-fill both do at their real call sites. Because this function is diagnostic-only (backs `validateBoundaryQuality`'s `report-all` calibration harness, `syncContracts.ts`), every `validateBoundaryQuality` reading on a seam-exempted pair has been wrong since it shipped — it reads as "used the fallback" in cases where the real seam exemption (in `snapCoveredBoundaries`) would have found a silence candidate. Does not affect any committed boundary — `boundaryUsedFallback` never writes timing, only measures it. Scheduled for v2 Phase 7; self-resolves if v2 Phase 6 deletes the exemption instead.

---

## Documentation Map

> What each doc in `docs/` is authoritative for, so a future session knows which to read for live guidance vs. which is point-in-time evidence.

| Doc | Authoritative for | Status |
|---|---|---|
| `docs/sync-pipeline-v2-plan.md` | The accepted 4-stage sync-pipeline restructure — the live plan for all future sync-timing work. Revision 2 (2026-08-03) adds stage contracts (Part J: IN→1, 1→2, 2→3, 3→4, OUT), per-stage lock gates + Stage Lock Status (Part D), the Stage 1 Transcript Inspector (Phase 1b, blocking), the real-test-data corpus inventory (Part D.0), the Russian descope (H), and the adversarial-audit revisions (Part K). | **Live plan, accepted 2026-08-03 (Revision 2 same day), pre-implementation.** |
| `docs/sync-pipeline-contract-plan.md` | The Pipeline Contract Program's assumption tables and risk register (R1-R14) for the OLD 7-stage pipeline — not restated anywhere else. v2's Part J maps R1-R14 onto the new 4-stage contracts (which are structurally impossible, which survive). | **Paused**, superseded in sequencing by v2 (its validator concepts absorb into v2 Phase 7; its severity taxonomy is absorbed into v2 Contract OUT). |
| `docs/verification-baseline.csv` | The forty-boundary script-word-keyed verification set's per-phase verdicts (columns: script-word key, project, case_type, verdict, phase label, date) — the only trustworthy correctness record across the v2 programme; every key verified unique (Phase 0 closure, 2026-08-04). Append-only across phases; never edit a superseded row. | **Live record**, updated at every phase's verification pass. |
| `docs/boundary-drift-investigation.md` | The word-shift defect's full evidence (11 ear-verified cases), the two failed fix attempts (FENCE, QUIET) and why each failed, and a DO NOT RE-INVESTIGATE list of dead hypotheses with measurements. | **Live reference** — read before any word-shift-adjacent work; addressed going forward by v2 Phases 3+5. |
| `docs/sync-redesign-audit-report.md` | Point-in-time audit findings that fed into the v2 plan. | **Point-in-time evidence**, retained, not live — findings absorbed into `docs/sync-pipeline-v2-plan.md`. |
| `docs/audit-verification-2026-08-03.md` | Point-in-time verification of the above audit against a specific HEAD sha. | **Point-in-time evidence**, retained, not live — same status as the audit report above. |
| `docs/history.md` | Completed-work history/archive (rewrites, decisions, implementation records) once a body of work closes out. | **Archive** — not a plan, never edited for future direction. |
| `CLAUDE.md` | Architecture, conventions, and invariants — current (pre-v2) behavior for every sync service, kept accurate until each v2 phase actually lands. | **Live reference.** |
| `project-state.md` (this file) | Current status, active tasks, deferred bugs/polish, key invariants. | **Live reference**, updated every session. |

---

## SaaS Readiness Tasks

> Items required before public launch or multi-user distribution. Not scheduled — tracked here so they aren't forgotten.

- **Backend proxy for API keys** — Pexels/Pixabay/Coverr keys currently in JS bundle (VITE_ prefix). Required before public launch.
- **Auth layer** — No authentication; open access. Required for multi-user.
- **LGPL ffmpeg swap** — Current sidecar is GPL (libx264). Swap for LGPL-only build (OpenH264 or commercial x264 license) before public distribution.
- ~~**4K export validation**~~ — **moot as of 2026-07-22.** 4K was fully removed from the UI and the `ResolutionTier` type by the Project Settings + Aspect Ratio work (see Active Tasks) — only 720p/1080p exist now. If 4K is ever reinstated, it needs dimension decisions for all 3 aspect ratios (`resolutionConfig.ts`'s `RESOLUTION_TABLE`) plus this same macOS/Windows validation pass, at that time.
- ~~**playbackSpeed UI re-expose**~~ — **resolved 2026-07-19.** UI was re-exposed via `SpeedBadge.tsx` (1x/2x/4x/8x cycling, click + ArrowLeft/ArrowRight) — no longer an open item.
- **Restrict `fetch_url_bytes` with a domain allowlist (SSRF hardening)** — currently fetches any URL passed from the webview; acceptable for internal single-user use, required before public launch. `lib.rs`
- **Download-on-first-use for the whisper model (Phase 2a, 2026-08-04)** — `ggml-large-v3-turbo.bin` (~1.51 GiB on disk, measured) currently must be bundled or manually provisioned the same way `ggml-base.en.bin` was; `tauri.conf.json`'s `bundle.resources: {"models/*": "models/"}` glob-bundles the whole `src-tauri/models/` directory, so an unmodified distribution build would ship ~1.65 GiB of model weight. Required before public distribution: fetch + progress UI + SHA256 checksum verification + storage-path resolution, replacing the bundle. Until built, `ggml-base.en.bin` must be removed from `src-tauri/models/` before any real `tauri build` (nothing references it after the Phase 2a swap). See `docs/sync-pipeline-v2-plan.md`'s H.9.

---

## Key Invariants

Non-negotiables. Future work — especially the Architecture Shift active task — must not break these without a deliberate, documented decision.

- **(a) Sync timing is regression-locked.** `src/services/syncTiming.test.ts` (154+ vitest tests in this file alone) plus regression tags protect the sync/anchor timing pipeline. Active bisect target: `sync-known-good-2026-07-29` → `bd9e919` (post-sync-rewrite baseline). Historical: `sync-known-good-2026-06-20` → `bab79b0` (pre-rewrite).
- **(b) Σ content-segment duration = voiceoverDuration.** This applies to content segments only — every `VideoSegment` in `project.segments` is content (no in-array headings exist as of Path B Phase 7, 2026-07-08). Total content-segment duration must always equal the voiceover's duration. Transition overlaps cancel pairwise by construction (Path B cross-fade design — see `docs/history.md`'s Decisions Log, 2026-05-25), so this holds without special-casing `App.tsx`. Headings are excluded entirely from this invariant — they own no timeline seconds (see (c)). This isn't theoretical: removing `splitAudio` in Heading Round 5 broke an earlier version of this invariant and cost 4 rounds of drift-bug fixes before headings were rebuilt as pure overlays.
- **(c) Headings are a separate top-level `HeadingOverlay[]` layer (`project.headings`), not segment-array entries.** Path B (see `docs/history.md` → "Path B — Separate Heading Layer — Design Decisions (Archived)") replaced the old in-array `isHeading`/`headingConfig` system entirely in Phase 7 (2026-07-08) — `VideoSegment` carries no heading fields anymore. A `HeadingOverlay` has a fixed absolute `time` that never moves on re-sync (a clamp+`needsReview` flag handles a re-sync that shrinks past it); it participates in no segment timing math and is composited on top of whichever segment(s) fall within its time range at render/export time.
- **(d) Transcription cache validity is keyed by file identity, not asset id.** `getFileIdentity(file) = \`${file.name}|${file.size}|${file.lastModified}\`` (`src/services/syncEngine.ts:216`), cached as `Project.lastTranscribedFileIdentity` (`src/types.ts:215`). Necessary because every file-stage event mints a fresh `Asset` id even when the user re-picks the identical file — id/reference equality can't catch a re-stage, but name+size+lastModified can.
- **(e) `anchorSource` provenance only ever moves one direction.** `'whisper'` = precise audio alignment; `'estimate'` = character-weight approximation that Whisper can still realign later. An anchor may be demoted `whisper → estimate` but is never promoted back, regardless of text changes (enforced by `syncTiming.test.ts`).
  - *Post-3c follow-up note — closed 2026-06-24 (post-3d-2):* `anchorSource` is confirmed effectively write-only — no production code branches on `'whisper'` vs `'estimate'`. Still written by `parseProjectData`, `applyAnchorBasedTiming` PASS 1, `distributeSegmentTimes`, and `handleInsertHeading`. Now documented directly in the `anchorSource` doc-comment in `src/types.ts`; no further cleanup planned.
- **(f) Adjacent covered segments are contiguous.** `startTime[i] + duration[i] === startTime[i+1]` for every adjacent pair produced by `snapCoveredBoundaries` (`snapBoundaries.ts`). Enforced unconditionally since the 2026-07-31 window-overlap regression fix (a boundary write followed by a duration-floor `Math.max` could previously leave `curr` extending past the just-written `next.startTime`) — Timeline.tsx's absolute-positioned segment cards depend on this holding, since each card's position is now a direct function of its own `startTime` rather than an accumulated sum of prior siblings' widths.

---

## Open Questions

No open questions.

---

## Quick Stats

| Metric | Value |
|---|---|
| `src/App.tsx` LOC | 4,369 (measured via `wc -l`) |
| Branch status | `webgl2-effects-engine` @ this commit (tag `clean-baseline-2026-07-31`, immediately after `abb642c`), **committed, not pushed** — ahead of `origin/webgl2-effects-engine` |
| Project persistence | Per-project scoped: `kinetix:project:{id}:v1` + registry `kinetix:projects:v1` in localStorage (legacy single-project key `kinetix:project:v1` retained for one-time migration only) |
| IndexedDB | `kinetix-assets` DB v2, store `assets-v2`, compound keyPath `['projectId','id']` (legacy v1 store retained for migration) |
| Total dependencies | 6 prod + 12 dev |
| Export codec | H.264 video + AAC audio, MP4 container |
| Export engine | Two gated paths as of 2026-07-22 (`useExport.ts`'s `isWebCodecsExportGateOpen()`): **default** — WebCodecs+WebGL2 worker path (`VideoDecoder`→GL composite→`VideoEncoder`, native ffmpeg sidecar for mux-only), toggle ON on every platform; **fallback** — legacy native ffmpeg sidecar (evermeet.cx 8.1.1 static build, GPL) full per-frame canvas pipeline via Tauri `tauri-plugin-shell`, used when the capability probe fails or the user toggles off. See `docs/history.md` → *WebCodecs + WebGL2 Worker Export — Implementation Record*. |
| Export speed — WebCodecs path (default) | Step 8 synthetic effects-heavy benchmark: 194s → 6.8s (**~28×**). Real projects: **~2.3×** — the honest number, not the synthetic one (GPU upload/readback cost in the Worker+OffscreenCanvas regime on WKWebView narrows the real-world win). Verified macOS Intel x86_64 only; macOS arm64/Windows unverified. Tier 1/C segments (plain, or not GL-expressible) run at legacy speed regardless. |
| Export speed — legacy path (fallback) | **Stale — pending re-measurement.** Figures predate the 2026-07-09 worker-pool PNG encode + raw-binary IPC write speedup (commit `cd7ea2b`); no post-fix benchmark has been run yet. macOS Intel (x86_64): ~10× realtime (120s for 12s of output) as of the base64-IPC-only pipeline; Windows: ~6× realtime (6 min per 1 min of video, measured on brother's PC); macOS arm64: pending measurement |
| Test count (`vitest`) | 1281 — down from 1284 via Phase 1's deletion of 14 tests exercising the deleted duplicated gap-fill (`docs/sync-pipeline-v2-plan.md`'s Phase 1), not a regression; see `docs/history.md` for the per-item breakdown of the earlier 1245→1284 growth |
| Frontend bundle size | 505.86 kB / 152.74 kB gzip main bundle (measured 2026-06-22; no wasm in bundle — ffmpeg is a sidecar binary) |
| Lazy chunks | StockSearchModal 8.79 kB · jszip 95.87 kB |
| ffmpeg sidecar binaries | 76 MB (x86_64-apple-darwin), 48 MB (aarch64-apple-darwin), 97 MB (x86_64-pc-windows-msvc) — all gitignored; see `src-tauri/binaries/README.md` |
| Transition enum values in UI | 10 (only implemented transitions shown) |
| Filter names in UI | 26 (only implemented filters shown) |
| AnimationType values rendered in export | 12 (all applied via `canvasAnimations.ts`) |
