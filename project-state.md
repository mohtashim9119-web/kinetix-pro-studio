# Kinetix Pro Studio — Project State

> **Purpose:** the situation report — perishable status only, always true as of right
> now. Six fixed sections (Current State, Active Workstreams, Open Decisions,
> Next Action, Rulings In Force, Deferred Planned Items); don't add a seventh. **Never
> put here:** task-level detail (checkbox lists, per-task notes) — that's
> `docs/work-in-progress.md`, which also holds each active workstream's own
> WS-specific rulings and deferred/known-bugs list; durable rules/invariants — that's
> `CLAUDE.md`; anything completed — remove it, don't mark it done, its record lives in
> `docs/history.md`. **Cap: ~250 lines.**

---

## 1. Current State

| Field | Value |
|---|---|
| Branch | `main` (trunk; `webgl2-effects-engine` tracks it, name is historical) |
| HEAD | WS1 Session AN — **widening the anchor search one sentence group recovers arm F's five fallback seams and beats it on every measured axis, on real audio, on both v6 and 173.** Gate committed to git (`c617a0f`) **before arm H's planner code existed and before any alignment ran against real audio**. Step 2 measured the edge-accuracy budget FIRST (no new planner): all 67 of arm F's beyond-±50ms v6 boundaries attributed to a governing chunk edge, **Pearson r = 0.876** between edge error and boundary error, but the inferred budget curve is **NOT STEEP** (60.7% of substitutable edges carry half the total error mass) — correctly bounding expectations to "real but partial progress," not closure. **Arm H** — one variable from arm F: an additional one-group-wider anchor search, tried ONLY at the 5 seams arm F's own two-group window couldn't resolve — recovers all 5 seams (cut displacements 3.46–30.1s from arm F's own cuts, larger than expected and flagged as a risk before the real run). MEASURED against real ONNX FA output (v6): oracle regressed **68 → 46** (−32%), peak |mean decile Δ| **3.249s → 0.349s** (both DIED, 9× tighter), mean FA confidence **0.8356 → 0.8882** (above production's own 0.8398), all **3 of 3** open defects land for the first time in this workstream (`231_slowing_pace` included, at its ear target exactly, confidence 0 → 0.9988), implied R-AS precision **2.86% → 6.12%**. HARD FAIL 3 (regressed > arm F's 68) did not fire. Repeated unchanged on **173** (Step 5's condition met): regressed 40 → 31 against arm C, though **173 shows no arch at all** to begin with (arm C's own peak, 3.854s, already under the DIED band) — reported as a finding, not a failure. **Found and corrected a defect in this session's own pre-registered adjudication function** — a fallback branch mechanically read this genuinely strong result as a negative; reported plainly rather than silently patched. **Gate verdict: arm H is the strongest S2-family result yet on nearly every axis, but is NOT a shipping candidate** (46/447 v6 boundaries still beyond ±50ms; 6.12% implied precision vs the 50% ship cap). **No rule added, deleted or re-tuned; no arbiter rebuilt; no per-row constant; nothing shipped to the production default.** Full detail: `docs/work-in-progress.md` §11p, `sync-pipeline-v2-plan.md` Part AH. |
| `vitest` (`npm test`, `--exclude '**/.claude/worktrees/**'`) | 2561 passed / 77 skipped / 0 failed — +18 from WS2 Step 13 (`ManageModelsModal.test.tsx` 7→15; `onnxruntimeBundle.guard.test.ts` widened for the multi-target manifest), re-measured 2026-08-27 |
| `tsc --noEmit` (= `npm run lint`) | clean |
| `cargo check --features fa-inference` | clean |
| `cargo clippy --all-targets --features fa-inference` | not re-run this session (no clippy-relevant pattern touched beyond what `cargo check --features fa-inference` already confirmed compiles clean) — was clean, 4 pre-existing warnings, 0 new |
| `cargo test` (default, no feature) | 157 passed / 0 failed / 1 ignored — +10 from WS2 Step 13 (`model_download.rs` gained its first-ever tests: 5; `models.rs` gained 5 status-detection regression tests), re-measured 2026-08-27 |
| `cargo test --features fa-inference` | not re-measured this session (no test added under that feature; `cargo check --features fa-inference` confirms the generalized ORT target table compiles) — was 216 passed / 0 failed / 24 ignored |
| Golden replay (`scripts/phase4-handoff-replay-sync.test.ts` + `phase4-fa-replay.test.ts`) | 6/6 byte-identical — the blind spot stands: it never reaches the chunk planner, FA, or any rule stage, so nothing about arm H's result is visible to it |
| FA Zero-Defect Register (`scripts/phase4-fa-replay.test.ts`) | **6 open**, unchanged — Session AN opened and closed none. The separate operator-attested open-defect register stands at **5** (v6 213-214/230-231/446-447, 173 5-6/106-107), also unchanged — though `231_slowing_pace` now lands under arm H, a measurement result, not yet a register/rule change |
| `faAnchors.ts` sha256 | `b61e94cb6ac61a3f8f22ce076ac55440227f4d4b5aef0c6d6aa980035db7380c` — unchanged since Session E |

All six floors re-measured directly 2026-08-24 (WS1 Session AN). The only `src/` change is one additive branch (`'anchor-widened'`) on `faChunkPlan.ts`'s existing `S2EdgePlacement` union, no existing arm's code path touched; everything else is under `scripts/` and `docs/`. `snapBoundaries.ts`, `silenceDetector.ts`, the Hirschberg aligner (`whisperService.ts`), `docs/history.md` and `scripts/fixtures/phase4-baseline-*.csv` are all untouched, and no new repo-root file was created. App status: shipping desktop app (Tauri DMG/installer, native ffmpeg sidecar export, no server/web hosting). Target users: YouTube creators, initial internal use across 5–10 channels. Repo: `github.com/mohtashim9119-web/kinetix-pro-studio`.

---

## 2. Active Workstreams

Task-level detail lives in [`docs/work-in-progress.md`](docs/work-in-progress.md), not here.

- **WS1 — Sync Pipeline (forced-alignment timing-source upgrade):** **WS1 Session Y (2026-08-22)** pinned `fa_onnx.rs`'s `load_session` to single-threaded/sequential/deterministic ONNX Runtime execution and MEASURED 3 independent runs byte-identical on real 173 and v6 production audio (a permanent, `#[ignore]`d gate test); the mutation control (same window, pre-fix unpinned construction) was ALSO byte-identical on this hardware, so Session X's live-vs-regen divergence stays INFERRED, not confirmed. Tested the script-anchored word-gap placement hypothesis on real ground truth (173's 5 defects + 19 controls, v6's 15 register rows) — mixed result (3/5 defects confirm, biased off the geometric midpoint; 2/5 refute; v6 contaminated by near-zero-confidence anchor words) — ships nothing, `faAnchors.ts` untouched. The propose/arbitrate rule-stage rebuild (below) was designed but deliberately not implemented this session — no driving rule change from the placement result, and the rewrite's regression risk to a hash-pinned, 2465-test-covered module warrants a dedicated session. Full detail: `sync-pipeline-v2-plan.md`'s Part T, `docs/work-in-progress.md` §§11a-11b. Phase 3 (= Task 5) is **PRODUCTION PATH WIRED, gate PER-PROJECT, DEFAULT OFF**. The FA gate's default flip to ON is deferred to the final act of Stage 1 (ruling R-AD), gated on an **EMPTY Zero-Defect Register** — currently **8 open rows, all Class A (3) + Class B (5)**; full detail and row IDs in `docs/work-in-progress.md` §11. WS1 Session V (2026-08-22, Part 1) closed the other 7 (the five R.12 fixture-scoped rows, the `266_forty_one_burden` live-path row, and the reopened `383_sixty_four`) against a fresh run-id-stamped live bundle plus an operator A/B ear pass — `status: 'fixed'` in `KNOWN_BAD`, not converted to `CLOSED_BY_POSITIVE_ASSERTION`, because that requires matching the frozen fixture CSV, deliberately still unregenerated. `266`'s "regression" classification (788.65→788.75) is REFUTED: 788.75 is now the operator-confirmed correct value. Session V Part 2 (a Class A/B attribution-side detector) remains ON HOLD, not started. **WS1 Session W (2026-08-22, capture only)** froze the pre-fix state of the 173 project after a live sync surfaced an apparent wrong cut at the segment 6-7 boundary: a fresh run-id-stamped capture found the 6-7 boundary matches the live app exactly and is attribution-clean (no word crosses the seam) — if still wrong on a re-listen it is a placement, not attribution, defect — plus one unrelated fidelity-gate divergence (`vessel_damage_clue`, live 174.74 vs. this session's fresh regen 172.91, both real silence midpoints) not yet explained. Ear list: `docs/history-2.md`'s Session W entry. **WS1 Session X (2026-08-22)** ingested the operator's full 173 listening pass (24 verdicts: 19 correct, 5 real defects — `docs/history-2.md`'s Session W entry's own 21 rows plus 3 off-list defects caught in a fuller listen-through) into `scripts/ws1-ear-pass-ledger.ts`, measured every live boundary signal's precision/recall against that ground truth for the first time (still-playing 20% recall / 17% precision; silence-distance>20ms 20% recall / 6% precision; every other rule 0 fires on this corpus this run), found the v6-tuned silence-midpoint geometry does NOT generalize to 173 (83-94% false-positive rates), searched for and did not find a clean suppression-class discriminator ("R-MD" — negative result, nothing shipped), and chased the `vessel_damage_clue` (45-46) live-vs-regen divergence Session W deferred to a named mechanism: Whisper tokens are byte-identical between the two captures, but FA (ONNX) word-level output diverges substantially for the same tokens — most likely ONNX Runtime's own unpinned thread pool, scoped to FA-gated projects only (opt-in, default OFF). No register row opened or closed. See `docs/work-in-progress.md` §11, Session X entry. **WS1 Session Z (2026-08-22)** chased the `vessel_damage_clue` (45-46) divergence further: REFUTED the chunk-plan hypothesis by direct measurement (three real 173 chunk plans — 118/119/126 chunks — one pair genuinely splits right where the divergent word sits and one doesn't, exactly the predicted edge-vs-mid-chunk signature, but all three give byte-identical FA word output at that word); ran 7 total reproduction trials (idle/CPU-saturated/concurrent) plus a third, strictly stronger determinism mutation (forced parallel execution, 8+4 threads) — all byte-identical, so the mutation gate is now stated plainly as INERT on this hardware, not a proven-armed regression gate. The mechanism stays UNEXPLAINED (not retracted) after ruling out chunk plan, engine threading, and audio identity. Adjudicated 45-46 through the complete rule stage on the frozen capture: commits 172.91 (a pure silence-snap decision, no rule involvement) — a genuine defect vs. ear ground truth (174.74) on that capture, named but NOT added to `KNOWN_BAD` (needs a fixture regeneration this session's constraints bar). Measured v6's boundary-adjacent FA confidence at 2.25x lower than 173's (44.3% vs. 19.7% below 0.01), found all 8 open Class A/B rows cluster in the near-zero band on both their committed and ear-correct anchors, and specified a GEOMETRIC low-confidence fallback (0.056). Re-tested the word-gap placement hypothesis with a derived ~20ms pre-roll — both previously-refuting rows still refute — ships nothing, a second negative. No register row opened or closed, `faAnchors.ts` untouched. Full detail: `sync-pipeline-v2-plan.md` Part U, `docs/work-in-progress.md` §11c. **WS1 Session AA (2026-08-22)** tested whether Session Z's near-zero FA confidence finding actually signals unreliable placement, and found it does not: the sub-threshold population's function-word share (55.6%) sits *below* the control population's own (64.5%), refuting the leading "mostly low-energy function words" hypothesis at the composition level, and an independent-arm validation against Whisper (same run-id bundle, occurrence-rank matching tried and abandoned as a dead end before settling on windowed nearest-time matching) found near-zero-confidence words agree with Whisper equal-to-better than high-confidence controls, in both content and function sub-classes separately — "the aligner is uncertain but not measurably wrong," Session Z's own headline stated as a misreading, per the brief's own anticipated third outcome. No confidence-based guard was wired: the evidence above doesn't support one, and independently, the one call site that would need it (`faAnchors.ts`'s silence-snap) is under this session's own hard no-touch constraint; a `LOW_CONFIDENCE_NO_OP` event schema is specified but not implemented. Audited all six named rules for FA-confidence dependency: only R.10/R.11 consume it at all, both as absence-of-evidence (never placement trust, so this session's finding doesn't implicate either), R.10 stays evidence-backed (850x margin) and R.11 stays under-evidenced (its own pre-existing 2.8x-margin self-assessment) — the one rule warranting re-derivation; R.5/R.12/R.13/R-U consume none. No `KNOWN_BAD` row touched. Word-gap re-test restricted to non-collapsed anchors dropped n from 5 to 3 (both dropped rows were confirming) — same two rows still refute, a third negative across three independent sessions. Tractability verdict on the 8 open Class A/B rows: addressable with current FA output, not blocked on alignment quality — every row already has a named, detector-design-level mechanism. Zero source changes this session. Full detail: `sync-pipeline-v2-plan.md` Part V, `docs/work-in-progress.md` §11d. **WS1 Session AB (2026-08-22)** re-derived R.11's two corpus-fitted constants after finding the session brief's "six ear-confirmed firings" premise did not hold (only 2 of R.11's 6 live raw firings carry an ear pass scoring R.11's own proposed correction correct — full detail in Part W). `R11_MIN_FIT_DEVIATION` (1.3093) reaffirmed unchanged — re-deriving far enough to reach Class A's `214_solitary_fire` near-miss would also admit four Class B rows and an R.12-owned false positive sharing the same fitDeviation floor as a genuine defect, both explicitly out of scope. `R11_MAX_SPAN_WORD_CONF` re-derived and SHIPPED (1.0835e-2 → 3.9362e-3, margin 2.8x → ~4.02x, monotonically safe), all 13 regression pins reproduced byte-identical, mutation empirically RED-then-GREEN. Seven amplitude/energy discriminator candidates tested against real audio for Class A (214/447) — best reaches 40% precision, corpus-confounded, negative result, ships nothing. Session Z's n=3 onset-lead-in extended to 41 ear-confirmed controls — order of magnitude holds, precise range does not; no new cut point adopted, guard unwired. Class B's amplitude-floor deficits recorded verbatim for Session AC — no Class B code touched. Full detail: `sync-pipeline-v2-plan.md` Part W, `docs/work-in-progress.md` §11e. **WS1 Session AC (2026-08-22)** re-measured all 17 tracked register rows against a fresh live-bundle capture and found zero drift beyond Session AB's own report (8 open Class A/B rows and 7 Session V closures all byte-identical to their recorded values; the two historical R.11 members unchanged from Session AB's finding). Found none of the 8 open rows has genuine ear evidence behind its proposed correction — the register's `session-p-live` ledger sitting is a same-session self-transcription, not an independent listening pass — and produced a fresh ear list covering all 8 (`docs/history-2.md`'s Session AC entry). Wrote an explicit Stage 1 exit-criteria checklist (`docs/work-in-progress.md` §11f, 3 MET/6 NOT MET/5 WAIVED-WITH-REASON), flagging a probable unactioned lapse in the Spanish non-English-corpus written acceptance and confirming that neither Phase 2's placement model (three independent refutations, WAIVED-BY-EVIDENCE), Phase 3's rule-stage rebuild, nor the 5-tier WPM suite gate Stage 1 lock — only the Zero-Defect Register (8 open) and the unfinished Contract 1→2/inspector-threshold/regression-checklist verification do. Costed the WPM prerequisite (100-150 to 500+ rows of listening burden depending on tier scope). Zero rule changes, zero threshold tuning, `faAnchors.ts` untouched. Full detail: `sync-pipeline-v2-plan.md` Part X, `docs/work-in-progress.md` §11f. **WS1 Session AD (2026-08-22)** closed Session AC's ear-evidence gap: ingested a genuine operator A/B pass for all 8 open rows plus row-0/item-7 into `scripts/ws1-ear-pass-ledger.ts` (sitting `ear-verify-ad`, 0/9 to 9/9 A/B-grade evidence, every value unchanged from what was on record) and traced a stray `450.99` value for item-7 to a single Session P (2026-08-19) transcription slip, reconciled to the ledger's own original 451.03 (a supersession, not a correction) — the register itself (`phase4-fa-replay.test.ts`) has zero lines changed, still 8 open. Re-ran the Class A amplitude/energy discriminator against all 4 now-confirmed positives instead of Session AB's 2: precision fell to 0.154 (from 0.400), a 5-way leave-one-out confirming this is a real population property; full negative. Re-derived the Class B amplitude floor at the app's real native audio rate (48kHz, not the replay bundle's 16kHz capture Session R had already proven artefactual here) — recall reaches 5/5 but one never-audited boundary (`008_unknown_void`) newly fires, so it is NOT SHIPPABLE, one ear check short of clean. Item-7 confirmed unreachable by fitDeviation, silence-distance, and now amplitude/energy alike; the attribution-level fix it needs is named (per Session R), not built. Zero rule changes, zero threshold tuning, `faAnchors.ts` and the register both untouched. Full detail: `sync-pipeline-v2-plan.md` Part Y, `docs/work-in-progress.md` §11g. **WS1 Session AE (2026-08-23)** shipped the first two boundary-correction rules since Session T — `src/services/faAnchorTrustGate.ts`, R.14 (smeared-anchor placement) and R.15 (tail attribution) — after an interval-word census over all 645 committed boundaries found that the defect population splits on a single token-ordinal integer rather than on any acoustic signal, and splits the OPPOSITE way to the brief's expectation (attribution defects are 173's, placement defects are v6's). 14 fires across three corpora, 10/10 precision on ear-scored rows, zero false positives against all 37 ear-CORRECT controls, all three constants GEOMETRIC and none fitted to the firing set (sensitivity flat at ±10%), mutation gate M17 red on all eight mutations. Register 8 → 14 → 7 open (six rows added — five of them Session X's 173 defects, which the register had never carried — seven closed against live); `152_frozen_brush_mice`/item-7 reached at residual 0.000s, so Stage 1's lock is no longer blocked on it. Native-rate decode measured and found already shipped: the 16 kHz→native movement census has an empty >100 ms band in all three corpora and moves all 15 defect rows by exactly 0.000s, so Class B is not closed by rate. Golden replay 6/6 byte-identical. Full detail: `sync-pipeline-v2-plan.md` Part Z, `docs/work-in-progress.md` §11h. **WS1 Session AG (2026-08-23)** built S1 (`faChunkPlan.ts`'s trailing-silence text fold, `foldPhantomTails`, DEFAULT OFF) and drove it through a real ONNX re-run on all three corpora: dropped the phantom-tail rate 66.1%→16.7% (v6)/38.7%→17.6% (173)/60%→0% (spanish), collapsed R.14's v6 firing count 11→1, scored CORRECT 10/13 on attributed defects with 0 worsened, and landed seam 230 (the one seam with independent two-arm ground truth) exactly at 682.740. Not shipped: it also moved two ear-verified CORRECT controls off their values. Also found (reported, not fixed): 173's stored chunk plan did not reproduce (126 vs 119) and golden replay reaches neither the chunk planner nor FA nor any rule. Full detail: `sync-pipeline-v2-plan.md` Part AA, `docs/work-in-progress.md` §11i. **WS1 Session AH (2026-08-23) REJECTED S1** — see HEAD line above for the full result; it is the current state of this workstream, not a superseded step. Full detail: `sync-pipeline-v2-plan.md` Part AB, `docs/work-in-progress.md` §11j. **WS1 Session AI (2026-08-23) BUILT AND MEASURED S2** — see the previous entry for the full result: the phantom-tail mechanism is structurally eliminated but the fix causes 36 ear-verified-control regressions, so it is neither shipped nor deleted. Full detail: `sync-pipeline-v2-plan.md` Part AC, `docs/work-in-progress.md` §11k. **WS1 Session AJ-0 (2026-08-23) installed the machine oracle and solved `vessel_damage_clue`** — see HEAD line above for the full result. Full detail: `sync-pipeline-v2-plan.md` Part AD, `docs/work-in-progress.md` §11l. **WS1 Session AK (2026-08-23) controlled the R.5 confound and REJECTED global S2** — see HEAD line above; excision is a contributing cause worth 14 exact control repairs on v6 but inert on the two zero-run corpora, and 173's 40 identical regressions under both arms show the surviving mechanism is chunk width, not recitations. Full detail: `sync-pipeline-v2-plan.md` Part AE, `docs/work-in-progress.md` §11m. **WS1 Session AL (2026-08-24) closed the width hypothesis NEGATIVE** — a period-strict 1–15s band regresses 363 v6 boundaries against arm C's 279 and production's 1, peak drift *rose* to −20.617s at half the chunk width, and the arch was traced to the anchor-based estimate's own error at r = 0.9940 by a reference independent of every arm. Full detail: `sync-pipeline-v2-plan.md` Part AF, `docs/work-in-progress.md` §11n. **WS1 Session AM (2026-08-24) CONFIRMED the arch's cause by direct intervention** — anchor-placed chunk edges (arm F) and oracle-placed edges (arm G, diagnostic only) both kill the arch (3.249s and 0.042s against the 19.155s arm C sat at) and cut oracle regressions 279 → 68 → 2, with zero ear-verified controls worsened by either arm. Full detail: `sync-pipeline-v2-plan.md` Part AG, `docs/work-in-progress.md` §11o. **WS1 Session AN (2026-08-24) RECOVERED arm F's five fallback seams and beat it on every axis, on real audio, on both v6 and 173** — see HEAD line above for the full result. Full detail: `sync-pipeline-v2-plan.md` Part AH, `docs/work-in-progress.md` §11p.

---

## 3. Open Decisions

- **Three defect classes remain open, and each needs a different answer — WS1 Session AE.** R.14/R.15 closed the placement and attribution classes; what is left is (a) the **wrong-landmark** class (`lethal_nature_hazard`, `gadget_decay` on 173 — committed on a real silence's midpoint that is simply the wrong silence, with RELIABLE anchors on both sides, so neither shipped rule claims them and no rule is designed); (b) the **ordinalDelta +1 mirror** class (`214_solitary_fire` — the incoming segment's own first word starts before the cut; widening R.14 to `>= 0` was measured and turns three ear-CORRECT controls into false positives); (c) two rows that are **IMPROVED but not closed** (`400_endless_dark` at +0.090s, `wall_split_path` at +0.310s — the latter provably unreachable by any script-anchored placement, its ear target sitting strictly inside a full-confidence word of the outgoing segment). Detail: `docs/work-in-progress.md` §11h.
- **Whether to relax R.14's reliable-onset guard needs one listening pass, not more analysis.** Relaxing it recovers `231_slowing_pace` but admits a 3.30s unverified move on v6 (`289_winter_predator_breach`) and two on Spanish, one landing exactly on a neighbouring ear-verified boundary. An ear verdict on those five boundaries decides it either way; nothing else will. Detail: `docs/work-in-progress.md` §11h's deferred list.
- **Rule-stage architecture: propose-then-arbitrate rebuild is scheduled, not started.** Rules currently mutate a shared array in sequence, so a conflict between two rules' claims resolves by ordering with no record a conflict occurred (this is what produced the R.11/R.12 collision R-AP closed in Session S). The scheduled fix — every rule emits a proposal against the origin array, an arbitrator resolves competing claims on stated ownership — would also absorb the R-AP performance cost below. Detail: `docs/work-in-progress.md` §11(f).
- **R-AP's measured cost (~2.70s per Apply Sync on the 447-segment v6 corpus) has a known fix, not yet built.** `computeUnscriptedRuns`'s Hirschberg alignment now runs 4 times per Apply Sync instead of 1 (once per rule stage that needs run structure); a memo on the pure function removes 3 of the 4 passes. Deliberately bundled with the propose/arbitrate rebuild above rather than bolted on standalone. Detail: `docs/work-in-progress.md` §11(h2).

---

## 4. Next Action

Rolling 3 — worked in order. **Maintenance rule:** when the first task completes it is removed, the list shifts up, and the next task from the roadmap's priority order is appended; the list is always exactly three.

1. **Owner scoring of the 24-row mover audit dossier** (`docs/ws1-sync-pipeline/stage1-mover-audit.md`) — the last gate on the live acceptance run and the FA-default flip. Not scored as of last update. (Item promoted 2026-08-25: the prior item 1, closing arm H's chunk-edge residual, is retired — the 2026-08-25 accuracy-bar decision froze further S2/chunk-edge research; current ~97-98% accuracy is accepted and remaining errors go through manual review. Detail: `docs/work-in-progress.md`'s Work in progress section, `docs/history-2.md`'s Session AN entry.)
2. **Slice 2 — re-derive the 50/50 silence-split rule.** Port `snapBoundaries.ts` + Apply-Sync plumbing (park-commit `210855d`) against current `main`. Still not started; will deliberately break the golden replay, budget a per-boundary review, never a blind re-baseline. Detail: `docs/work-in-progress.md` WS1 item 2.
3. **Give the rule stage its own regression coverage.** Golden replay stopped at `snapCoveredBoundaries` again: it stayed 6/6 byte-identical while arm F moved 71 v6 boundaries and arm H moved more still. Every rule-stage change since Session P has been verified only against live FA arms, which are slow, machine-specific, and not in the default sweep. The oracle now makes a cheap fixture-backed rule-stage test possible for the first time. Detail: `CLAUDE.md` §4 Testing, `sync-pipeline-v2-plan.md` AE.10.

---

## 5. Rulings In Force

One line each — full record in `docs/history.md`'s "Decisions Log — Dissolved from `docs/decisions/`" section (operative rule also in `CLAUDE.md` §4 Invariants). This section holds only cross-cutting rulings that apply beyond a single workstream; WS-specific rulings (currently WS1's) live in that workstream's block in `docs/work-in-progress.md` instead.

- **Model P (gapless partition)** — `project.segments` is a gapless partition; Model S (independently-positioned slots with legal gaps) is rejected. [`docs/history.md#the-model-p-ruling--official-and-locked-2026-08-07`](docs/history.md#the-model-p-ruling--official-and-locked-2026-08-07), full analysis [`#the-segments-invariant--ruling-document-2026-08-07`](docs/history.md#the-segments-invariant--ruling-document-2026-08-07), revert-scope context [`#the-model-p-revert--what-actually-happened-2026-08-07`](docs/history.md#the-model-p-revert--what-actually-happened-2026-08-07).
- **Last-segment right edge is locked, both directions, w.r.t. drag.** `segments[N-1].end === mediaDuration` is a hard invariant. [`docs/history.md#the-last-segments-right-edge--official-and-locked-2026-08-08`](docs/history.md#the-last-segments-right-edge--official-and-locked-2026-08-08).
- **A cancelled drag (`pointercancel`) discards, never commits.** [`docs/history.md#the-pointercancel-question--ruled-discard-2026-08-08`](docs/history.md#the-pointercancel-question--ruled-discard-2026-08-08).
- **Undo/redo design** — snapshots not patches, 20-state depth, page-reload persistence, lock-blocks-traversal policy. [`docs/history.md#undo--redo--design-2026-08-08`](docs/history.md#undo--redo--design-2026-08-08) (now a record of what was built, not a proposal).

**Task 5 rulings (2026-08-11).** WS1-scoped, registered here rather than `docs/work-in-progress.md` by explicit owner instruction. Their original supporting detail lived in `ws1-master-roadmap.md`'s "NEXT UP" block, deleted in the 2026-08-14 WS1 tracker consolidation (`docs/history-2.md`'s "WS1 tracker consolidation & close-out audit" entry) — the per-ruling "Detail" pointers below are historical and no longer resolve; each ruling's own one-line text above is the complete, standing statement of the rule.

- **R-D — Step T is not in Task 5.** Model distribution/on-demand-download is its own task, required before any release build, not before Task 5 (no release is imminent — WS1 finishes first). Task 5 itself resolves FA models via `app_local_data_dir` with a manual-placement fallback. (Detail was roadmap NEXT UP, "Files expected to change" — source deleted, see note above.)
- **R-E — Model P outranks R.5.** A forced-alignment wildcard span is assigned to the preceding segment; forced alignment may never emit a real gap in `project.segments`. Settles the open recommendation at `sync-pipeline-v2-plan.md:1441` in Model P's favor.
- **R-F — Decision 8 splits.** R.5's CTC-wildcard windowing mechanic is in Task 5/Phase 3's own scope; heading-assignment UI (Option A's on-screen behavior) stays Phase 5. (Detail was roadmap NEXT UP, "Out of scope for this task" — source deleted, see note above.)
- **R-G — `anchorSource` gains `'forced-alignment'`**, ordered above `'whisper'` (forced-alignment > whisper > estimate). Demote-only ordering is preserved; the value is set explicitly by the code path that produced it, never inferred. (Detail was roadmap NEXT UP hazards list — source deleted, see note above.)
- **R-H — Golden replay is extended before FA timing lands.** A forced-alignment input set and a second baseline are added while the diff against the existing baseline is still zero, ahead of any FA timing change. **Amended 2026-08-12 (R-Q): HALF SATISFIED** — the input set/first baseline landed, but the second pass ("FA swap run and reviewed per-boundary against that baseline") can't happen until Task 5 wires a real model; recorded as a hard precondition on that slice. (Detail was roadmap NEXT UP acceptance criteria, R-H status note — source deleted, see note above.)
- **R-Q — FA golden-replay fixtures are MMS-FA-derived and scoped to Viterbi/windowing fidelity only.** MMS-FA is barred from shipping (Decision 3); production ships jonatasgrosman per-language models instead, so the fixtures are not a reference for text normalization, vocab coverage, or word-drop behavior. Regenerating them against jonatasgrosman is an obligation on the FA wiring slice (Task 5), not on the text normalizer. (Detail was roadmap NEXT UP, R-H status note — source deleted, see note above.)
- **R-I — `-nfa` stays deferred until after Task 5 ships.** Independent by design, entangled with Task 5 at the measurement-baseline level. (Detail was roadmap §13 — source deleted, see note above.)
- **R-J — `preserveSegmentLocks` position is locked.** Stays at its current post-`autoMatchSegments` call site; must not be moved into `applyAnchorBasedTiming` under Task 5 or any later phase. (Detail was roadmap NEXT UP hazards list — source deleted, see note above.)
- **R-K — No release build until WS1 completes.** R-D (Step T, model distribution, kept out of Task 5) depends on this; if release timing changes, R-D must be revisited before Task 5 ships. (Detail was roadmap NEXT UP, "Files expected to change" — source deleted, see note above.)
- **R-L — Forced alignment runs natively in-process, compiled into the binary.** Out-of-process sidecars (Python, or any external runtime) are rejected permanently as a delivery mechanism for alignment — closes the runtime-delivery Option A (a distinct decision from Decision 8's heading-wildcard Option A, roadmap §4 Step V). `ffmpeg` and `whisper-cli` remain sidecars; R-L governs alignment only. (Detail was roadmap §13 — source deleted, see note above.)
- **R-M — `ort` (ONNX Runtime) is the forced-alignment runtime.** `candle` is rejected — no wav2vec2/CTC implementation exists in `candle-transformers`, and hand-writing the forward pass is out of scope. Accepted cost: a from-source onnxruntime build for `x86_64-apple-darwin` in CI, following the existing whisper-cli from-source pattern in the same workflow. Detail: runtime-spike measurement file, G3/G4.
- **R-N — R-L's packaging reading is DEFERRED, not decided.** R-L's "compiled into the binary" has two readings for `ort` specifically: static-link (single fat binary) vs. default/load-dynamic (in-process, but bundles a separate onnxruntime dylib). R-K means no build is being cut, and load-dynamic already compiles — both readings satisfy R-L's in-process requirement, so this is not urgent yet. Must be decided before Step T and before any release build — recorded as a blocker on both. (Detail was roadmap §13 — source deleted, see note above; `sync-pipeline-v2-plan.md`'s own Step T entry still stands.)

**Spanish-corpus acceptance ruling (2026-08-27).** WS1-scoped, registered here by explicit owner
instruction (same precedent as the Task 5 and R.1 ruling blocks above). The non-English-corpus
written acceptance's reopening trigger — void "the moment any Spanish-specific normalization or
alignment code ships" — fired in literal text when Phase 3b shipped Spanish cardinal normalization
(2026-08-15); unruled since Session AC flagged it (2026-08-22). Ruled via Option (a) of the three
options recorded at `sync-pipeline-v2-plan.md` Part AI.8: the owner personally ear-verified all 27
Spanish corpus boundaries following a live forced-alignment sync and confirmed 100% timing
accuracy across all boundaries. This satisfies the non-English-corpus acceptance criteria in full;
the lapse is closed.

**R.1 spec-hole rulings (2026-08-12).** WS1-scoped, registered here by the same explicit owner instruction as the Task 5 rulings above — full detail lives in `sync-pipeline-v2-plan.md` at R.1/R.4.

- **R-O — "Distinctive" (R.1(a)) is a measurable admissibility test: length ≥ `MIN_ANCHOR_WORD_CHARS` (3, seeded from C10's own ≥3-char definition) AND first canonicalized character not in `GLIDE_INITIAL_CHARS` (`{w, y}`, seeded from Step B's measured glide-initial finding). No stopword list, for any of the 5 supported languages. Resolves a conflict in R.1(a)'s own text: its justification cites Step B's PHONETIC glide-initial measurement, but its rule text said "not a function word" — LEXICAL, C10's definition. This ruling picks the phonetic reading; C10's lexical definition governs C10 only. Biased toward rejection deliberately: a rejected anchor only costs a longer run (bounded by `MAX_RUN_SEC`); a wrong anchor corrupts timing — the costs are not symmetric. Detail: `sync-pipeline-v2-plan.md` at R.1.
- **R-P — R.4 force-split selection** (when no admissible anchor exists within `MAX_RUN_SEC`): split at the longest detected silence inside the window; if the window has no detected silence, split at exactly `MAX_RUN_SEC`. The resulting boundary's provenance is recorded distinguishably from an agreed anchor. Never produces a gap (R-E, Model P). Detail: `sync-pipeline-v2-plan.md` at R.4.

---

## 6. Deferred Planned Items

### Polish Features

Owner-maintained — items are added or removed only on explicit owner instruction, never as a side effect of an audit.

1. Version snapshots — blocked on two open design decisions (asset-restoration approach, full-rewind-on-restore).
2. Auto-captions (reuse Whisper transcript tokens as a timed text layer).
3. Multi-user support — team accounts vs. staying single-user, revisit if demand materializes.
4. Sync loading screen — live 0-100% progress instead of the current static message.
5. Export quality — real color-space conversion + cross-segment drift correction (mux-time bt709 tagging already fixes the practical color mismatch; revisit only if a real issue surfaces).

### SaaS/Public-Launch Readiness

Not scheduled — required before public launch or multi-user distribution, tracked here so they aren't forgotten.

- Backend proxy for API keys — Pexels/Pixabay/Coverr keys currently ship in the client JS bundle (`VITE_`-prefixed).
- Auth layer — no authentication today; open access. Required for multi-user.
- LGPL ffmpeg swap — current sidecar (`libx264`) is GPL; swap for an LGPL-only build (OpenH264 or a commercial x264 license) before public distribution.
- Restrict `fetch_url_bytes` with a domain allowlist (SSRF hardening, `src-tauri/src/lib.rs`) — currently fetches any URL the webview passes; acceptable for internal single-user use, required before public launch.
