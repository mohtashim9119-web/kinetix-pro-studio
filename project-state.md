# Kinetix Pro Studio — Project State

> **Purpose:** Living source of truth for project status. Updated at the end of every work session.
> Distinct from `CLAUDE.md` — that file covers architecture/conventions; this file tracks where we are.

---

## Current State

| Field | Value |
|---|---|
| Last updated | 2026-07-19 |
| Current HEAD | `71fa4a0` ("Waveform rendered-image caching (Phases A-D): LRU mirror + IndexedDB persistence, identity gate, Phase D cleanup") on branch `webgl2-effects-engine`. **Since the prior `5115a4e` state described below: 14 commits landed**, none of them merged to `webcodecs-api`/`main` yet. In commit order: `9437747` (docs: logged Bug 2/3/4 + the edge-drag regression, recorded Phase 3's manual-verification-checklist completion — the same doc commit whose own "refresh HEAD" this table cell is only now catching up to, 3 days late); `f126465` (**Bug 2 fix** — WebGL2 preview zoom sudden-jump during transitions, per-layer transform restructure; full writeup already in the 2026-07-13 Decisions Log entry below); `993fef0` (**Bug 4 fix** — WebGL2 preview cold-load black screen, canvas-mount-aware effect deps; full writeup already in the 2026-07-13 Decisions Log entry below); `e1f6985` ("fix: stop caching rejected session promises in videoDecoderPool" — this is the fix the 2026-07-13 Bug 4 Decisions Log entry below referred to as "the still-uncommitted `videoDecoderPool.ts` change"; it is committed now); `12a1c32` (Windows large-export `STATUS_BREAKPOINT` OOM crash fix; full writeup already in the 2026-07-14 Decisions Log entry below); `2ca3f17` (WebGL2 **Phase 4** — per-segment color grading, dev-gated, ships inert; see Active Tasks below); `2015218` (WebGL2 **Phase 5 cutover** — GL path now the production default, dev gate removed, legacy CSS/Canvas2D transition path deleted — plus the same-day Effects-tab UI cleanup; Active Tasks below updated off its former "uncommitted" language); `0338cc2` (WebGL2 **zoom scale-rate control**, replacing the no-op `effectAnimationDuration` timer with a real shared `zoomScale.ts` rate module; Active Tasks below updated off its former "NOT YET COMMITTED" language — **manual real-app verification of this control is still owed**, see that entry); `7bd9871`/`063fc3c`/`de4c195` (three Windows-whisper AVX-crash/build-flag CI fixes — full detail in the two 2026-07-16 Decisions Log entries below; **still unverified on real hardware** per those entries, unchanged by this session); `f3d429e` ("Waveform rewrite Steps 1-5" — fixed a 2.5min sync freeze via chunked decode pipeline, async draw queue, reduced peak density 200→10/sec, ready-tracker, loading overlay); `e7efce5` ("Waveform peaks persistence + doc sync"); `71fa4a0` (**this session's commit** — waveform rendered-image caching: LRU mirror + IndexedDB persistence for rendered per-segment waveform PNGs, a pre-generation-bump identity gate in `App.tsx`, and Phase D cleanup wiring; user-verified via a 294/294 cache-hit trace on cold restart and Phase D confirmed in the real Tauri app — see the 2026-07-19 Decisions Log entry below; the reload-pacing sub-problem it does NOT fix remains open, tracked in `docs/waveform-image-cache-plan.md` Section 6). `tsc --noEmit` clean, `vitest` **632/632** as of `71fa4a0`. **Prior state (`5115a4e`), unchanged below:** four docs-only commits landed — `37a6c4d` (synced project-state.md/webgl-architecture-plan.md to close Bug 1), then a four-stage docs cleanup pass: `7bb14a1` (Stage 1 — corrected factual errors across CLAUDE.md/project-state.md/webcodecs-architecture-plan.md), `3b4b1c3` (Stage 2 — moved completed/historical content to `docs/history.md`), `a894615` (Stage 3 — trimmed the webgl/webcodecs plans to open work only, archived closed phases), `5115a4e` (Stage 4 — replaced the stale AI Studio boilerplate README). None of these four touched source code. **Prior state (`1121b76`), unchanged below:** since the previously-recorded `f52ab12`/`webcodecs-api` state below, work cut over to `webgl2-effects-engine` and landed (a) the WebGL2 effects-engine rebuild's **Phase 3** — preview integration, dual-gated behind capability + dev-toggle (`5a40cc6`/`d10a855`/`01bcd0d`, including a same-day WKWebView `texImage2D` perf-regression fix); (b) the **D7 transition-centering fix** (`94b42e4`/`09cf2e5`) — the transition window is now centered 50/50 across the A/B boundary, applied identically to preview and export; and (c) the **Bug 1 — WebGL2 preview video-video transition blend-gap fix** (`1121b76`, dedicated write-up in `docs/history.md`). Phase 3's own manual verification checklist is still un-run — see Active Tasks. Full WebGL2 rebuild detail lives in `docs/webgl-architecture-plan.md`. **Prior recorded state:** `f52ab12` ("docs: retain WebCodecs VideoEncoder investigation spikes (audit, b0-repro, mux-proof)") on `webcodecs-api`; three commits landed on top of the `c9c4085` state (itself unchanged from what the previous HEAD note below describes): (1) `d033cb1` — fixed same-URL transition-blend video-element thrashing by routing the incoming-segment blend render to an isolated `blendVideoCache` in `frameRenderer.ts` (see `docs/history.md`); (2) `cd7ea2b` — worker-pool pipelined PNG encoding (new `src/services/frameEncodeWorker.ts`, OffscreenCanvas-based) + raw-binary Tauri IPC frame write (new `ffmpeg_write_file_raw` Rust command, `TauriFfmpeg.writeFileRaw`), replacing the base64 round-trip on the per-frame export write path (see `docs/history.md`) — this is the OffscreenCanvas/Worker speedup `docs/phase-7-task-1-export-profiling.md` had flagged as the next export-speed candidate, now actually shipped, and is UNRELATED to the still-blocked "Export rewrite: WebCodecs pipeline" Active Task below (that task means swapping to `VideoEncoder`; this fix speeds up the existing ffmpeg/PNG pipeline both still use); (3) `f52ab12` — formally committed the three WebCodecs `VideoEncoder` investigation spike harnesses (`spike-webcodecs-audit.html`/`webcodecsAuditSpike`, `spike-webcodecs-b0repro.html`/`webcodecsB0Repro`, `spike-webcodecs-muxproof.html`/`webcodecsMuxProof`) that `docs/webcodecs-architecture-plan.md`'s 2026-07-09 B0 entries already narrate as "kept, not deleted" but which had actually been sitting untracked in the working tree since that investigation; also fixed a wrong DOM-lib type name (`HardwarePreference` → the real `HardwareAcceleration`) in two of them so full-repo `tsc --noEmit` is clean. `tsc --noEmit` clean and `vitest` **277/277** after all three commits (up from 229/229 — the intervening count growth predates this session; see the D16-related entries in `docs/history.md` for that history). The prior HEAD was `c66c922` ("fix: extract embedded digit runs from mixed alnum tokens in canonicalizeForAlignment (D16 percent/glued-token follow-up) + close years-verification"). That commit: (1) closes the D16 pair-reading **years-verification** task — a real end-to-end test confirmed all three pair-reading-form years ("nineteen eighty seven"/1987, "twenty twenty four"/2024, "nineteen ninety nine"/1999) sync correctly; combined with the earlier cardinal-form confirmation, both branches of `canonicalizeForAlignment`'s year-handling are now verified against real data. (2) Fixes the **"ninety seven percent" glued-token** failure surfaced by that same test: `canonicalizeForAlignment` gated digit-to-words expansion on a *whole-token* `/^\d+$/` test, so a token where whisper.cpp's `-ml 1` boundary heuristic glued a preceding function word onto the number with no space (e.g. `to97%` → tokens `["to97","percent"]`) never expanded the number and couldn't match the spelled-out script — DISTINCT from and upstream of the `aa12206` overshoot (that guard contains a bad match; this is the earlier miss that produces it). Fixed as a strict additive extension (split a mixed token on `/\d+|\D+/g`, expand each digit run via the existing path, emit letter runs as their own words; all previously-working shapes byte-identical). `tsc --noEmit` clean, `vitest` **229/229** (227 + 2 new). **User manually verified the glued-token fix end-to-end on real audio.** The prior committed state was `aa12206` ("fix: prevent low-confidence alignment matches from overshooting into next segment's word range (Whisper D16 follow-up)"). Since the pre-aa12206 HEAD note (`5f764a7`): a real-project follow-up bug was found in D16 (`cdc1eb1`) itself — a low-confidence match's `bestStart` could land far ahead of the search cursor (a spurious coincidental match against non-spoken caption text), anchoring that segment's `t1` past the next segment's true start and collapsing both to near-zero/negative duration. Fixed via an overshoot guard in `src/services/whisperService.ts` (re-anchors an overshooting low-confidence match to the cursor) plus a backstop monotonic-anchor clamp in `src/services/syncEngine.ts`'s `applyAnchorBasedTiming`, commit `aa12206`. Verified against the user's real transcript: overshoot fires on exactly the 5 genuinely-broken segments, zero false positives on the 8 previously-safe low-confidence segments, zero `[anchor] out-of-order` warnings post-fix (previously 2) — see the D16 write-up in `docs/history.md` and the 2026-07-07 Decisions Log entry below. `tsc --noEmit` clean, `vitest` **220/220**. On top of that, a docs-only cleanup had landed (`5f764a7`) — no functional change. Commits since the WebCodecs Bug-2 fix (`0a3f67c`, which this cell previously referenced): live per-tick outgoing-side transition rendering (Item 4 B1-B3, `9f4d276`/`73d1f54`/`10410f0`, all shipped and stable) — **B4 (overlay parity) was then implemented, manually tested, found NOT to fix the reported animation jump/disappear symptoms, and surfaced an unrelated React "Maximum update depth exceeded" loop in `usePlayback.ts`; B4/B5 are now PARKED/SUPERSEDED (not "pending") and all uncommitted B4 work was discarded back to clean B3 state — see the WebGL/WebGPU rebuild Decisions Log entry and Active Tasks below, and `docs/webcodecs-architecture-plan.md`'s Item 4 entry for full detail.** The WebCodecs preview migration (Phases 0–8) is complete: `VideoDecoder`-based decode pool + windowed decode-ahead + LRU replaced the dual-`<video>`-slot preview path, cutover to default on all WebCodecs-capable runtimes, with the legacy `<video>` path retained as capability-gated fallback — full detail and per-phase evidence in `docs/webcodecs-architecture-plan.md`. Post-cutover transition-flash-back (Bug 1, commit `3022706`) and animation hard-cut (Bug 2, commit `0a3f67c`) fixes landed on top. Earlier: export quality/perf (CRF 16, vignette removal, Tier 1 fast-path bypass — 3m44s→40s on a mixed 4-video/10-image project), UI smoothness fixes, Effects Tab Rebuild Step 8 transitions (10/10), Architecture Shift complete (2026-06-24). |
| App status | Shipping desktop app — Tauri DMG/installer, native ffmpeg sidecar export. No server, no web hosting. |
| Target users | YouTube creators — initial internal use across 5–10 channels |
| Repo | TBD |
| Restore tag | `sync-known-good-2026-06-20` → commit `bab79b0` ("chore: remove VO-DIAG/SYNC-DIAG debug logging") |

All foundational/export/desktop/sync work is shipped and stable, including the clean-slate re-sync Architecture Shift (closed 2026-06-24, commit `254ef1b`). Effects Tab Rebuild is complete (transitions 10/10, clip effects 7/7). Active work is feature tasks only — see Active Tasks.

---

## Active Tasks

- **WebGL2 effects-rendering engine rebuild (IN PROGRESS — Phases 0–5 COMPLETE and committed on branch `webgl2-effects-engine` (through `2015218`); Phase 6 next; not yet merged to `webcodecs-api`/`main`).** Decided 2026-07-07 (see Decisions Log) after the CSS/Canvas2D effects engine's Item 4 B4 patch failed to fix its target symptom and surfaced an unrelated infinite-render-loop bug — see this entry's Decisions Log writeup and `docs/webcodecs-architecture-plan.md`'s Item 4 entry for the full incident. Full status, evidence, and phase-by-phase detail live in `docs/webgl-architecture-plan.md`'s Progress Tracker (kept current throughout, mirroring how `docs/webcodecs-architecture-plan.md` was maintained) — summary here:
  - **Engine decision:** WebGL2 over WebGPU, GO confirmed empirically on both Chromium and the real Tauri WKWebView (Phase 0 spike).
  - **Scope** (revised from this entry's original "3 transitions" estimate once the plan doc was written): **4 transitions** (dip-to-black, dip-to-white, cross-dissolve, light-leak) + **2 animations** (zoom in, zoom out) + a **new color-grading feature area** (manual controls + auto-adjust, proposal not yet final spec).
  - **Phase 1 (core compositor module)** and **Phase 2 (effects build-out)** — COMPLETE. Included a same-day real-GPU pass-chain flip fix (commit `a92fc9a`) after Phase 1's mock-only tests missed a vertical-flip bug in the 2-draw render-target chain.
  - **Phase 3 (preview integration, dual-gated)** — ✅ **COMPLETE.** Code + automated tests complete (414/414); the full manual verification checklist (all 4 transitions, zoom-across-a-transitioning-boundary, pause/seek inside an active transition window, and drag-resize suppression) has now been run and confirmed passing by the user in the real Tauri app (2026-07-14 — the last two items, pause/seek and drag-resize suppression, closed out this session; the other two were confirmed 2026-07-13). A severe WKWebView-specific performance regression was found via real-app manual testing during this phase (`gl.texImage2D(HTMLCanvasElement)`, ~1800–2900x slower than Chromium, capped throughput at 17–27fps) and fixed same-day (UV-crop shader uniforms replacing a CPU-canvas pre-fit step) — re-verified on real WKWebView hardware (0.43–0.50ms/frame, down from 36–58ms). Three bugs surfaced during this phase's verification were each root-caused and fixed: the **video↔video transition blend gap** (`1121b76`, see "Bug 1" write-up in `docs/history.md`), **Bug 2 — zoom sudden-jump during transitions**, and **Bug 4 — black screen on cold load** (both see Decisions Log below). **Bug 3 (combined lag) and the timeline edge-drag regression remain open and unaudited** — see Deferred Known Bugs; neither was a Phase 3 checklist item, so neither blocked this phase's completion.
  - **Phase 4 (color grading)** — ✅ **COMPLETE.** Per-segment brightness/contrast/saturation/temperature (`VideoSegment.effectGrade`) + a one-shot histogram Auto-adjust + the GRADE UI (4 sliders, live preview on drag, Reset sliders, Clear grading, Apply to selected/all), built on the grade shader Phases 1–2 already shipped. Includes two review rounds on top of the initial build: Fixes A/B/C (contrast remap, sliders syncing from the active segment, a way to clear a grade), then the range + interaction audit (all four channels remapped to usable effective ranges, live preview while dragging, Reset live-writes). `tsc --noEmit` clean, `vitest` **456/456**. Verified via a real-GPU 81-combo pixel sweep of the real shader (0/81 unusable, vs 63/81 for the original raw feed) plus manual live-app testing. **Two caveats, both carried forward, neither blocking Phase 4:** (1) grading is **dev-gated and ships inert** — it renders only behind `import.meta.env.DEV && glDevToggle`, so a production build shows no grading until Phase 5 removes the gate; (2) the GPU sweep ran on **Chromium + ANGLE-Metal, not literally WKWebView** — literal Tauri/WKWebView verification of the grade path is an open item for Phase 5/6's broader verification, which matters given real-WKWebView testing has already caught two things Chromium missed (Phase 2's flip-parity bug, Phase 3's `texImage2D` perf regression). Full detail in `docs/webgl-architecture-plan.md`'s Progress Tracker.
  - **Phase 5 (cutover + legacy-path removal)** — ✅ **COMPLETE (committed in `2015218`).** The no-fallback step: the dev gate is removed, so the WebGL2 path (and therefore color grading) is what every build now renders, not just dev builds behind the toggle. The superseded CSS/Canvas2D transition path is deleted for the scoped effects — `useTransitionPreview.ts` removed entirely, plus the transition-overlay canvas and CSS zoom-wrapper cases in `PreviewStage.tsx`. Five transition slugs the GL engine never implemented (wipe, slide-push, glitch-rgb, whip-pan, zoom) are removed from the picker and now fold to cross-dissolve via a shared resolver map, matching export behavior for any already-saved segment using one. Full detail in `docs/webgl-architecture-plan.md`'s Progress Tracker. **Same-day Effects-tab UI cleanup (2026-07-16), same commit `2015218`:** transitions' off-state relabeled "Hard Cut" → "None" (slug unchanged); animations dropdown restricted to Zoom In/Zoom Out only (Ken Burns + the 5 filter-type entries hidden, not deleted, pending a future Filters tab); overlays dropdown reduced to None only with the section marked "Coming Soon" and disabled (no overlay slug was ever implemented on either render path).
  - **Phase 6 (export-readiness + scale validation)** — **NEXT, not started.** Includes the runtime-floor re-verification on macOS arm64 and Windows/WebView2 that Phase 0's single-machine spike could not do.
  - **Zoom scale-rate control (post-Phase-5 follow-up, same branch) — ✅ COMPLETE, fully tested, committed in `0338cc2`. Manual real-app verification NOT yet done.** Replaces the previously-audited no-op `effectAnimationDuration` "timer" field for the two GL zoom animations with a real per-segment `VideoSegment.effectAnimationScaleRate` (per-second rate) control, rendered through a new shared `src/services/zoomScale.ts` module now imported by BOTH the GL preview (`compositeParams.ts`) and the canvas export path (`canvasAnimations.ts`) — closing the prior duplicated-`0.05`-constant parity risk between the two renderers (previously kept in sync only by a comment, with zero shared code and zero export-side test coverage). Formula: Peak Scale = min(1.99, 1 + rate×duration); Zoom In/Out interpolate linearly between `1.0` and Peak Scale over the segment's own duration. Per-segment max-rate/slider-floor logic (`computeMaxRate`/`sliderMaxRate`/`capRateForDuration`) caps Apply-to-all independently per segment length, and a "capped" indicator surfaces on the BottomDrawer pill when a segment's rate is limited by its own length. Look presets (`lookPresetService.ts`) now carry an optional `scaleRate` for zoom-animation presets — additive schema, `kinetix:lookPresets:v1` unchanged, **no version bump**; old stored presets simply read `scaleRate: undefined` and applying one leaves a segment's existing/default rate untouched. `tsc --noEmit` clean, `vitest` **539/539** (new: `zoomScale.test.ts`, `canvasAnimations.test.ts` — previously zero export-zoom coverage — and `lookPresetService.test.ts`; ~15 pre-existing GL zoom assertions rewritten off the old hardcoded `0.05` constant). Full detail in `docs/webgl-architecture-plan.md`'s Progress Tracker. **Committed in `0338cc2`** — but this is a code-complete-and-tested commit, not a hardware-verified one: the actual GL preview + export zoom rendering, per-segment rate cap behavior, and look-preset carry have NOT yet been manually exercised in the real Tauri app. Flag this commit as unverified-on-hardware until that pass happens.
  - Rationale for the rebuild is unchanged from the original decision: the mixed DOM/CSS + Canvas2D rendering approach was the root cause of the Item-4 debugging saga and the slow per-frame export path; a unified WebGL2 renderer pairs natively with the already-shipped WebCodecs decode pipeline (`VideoFrame` uploads directly to a GPU texture, no conversion).

- **Export rewrite: WebCodecs pipeline.** Replace the canvas-seek + PNG-per-frame + ffmpeg-mux export path with `VideoDecoder` (sequential decode, no per-frame seeking) + `VideoEncoder` (direct frame encode, no PNG/disk round-trip) + ffmpeg mux-only (`-c copy`, no re-encode). Starts after the WebGL/WebGPU effects-rendering engine rebuild above is complete — not run in parallel. **Not the same effort as the 2026-07-09 worker-pool PNG encode + raw-binary IPC write speedup (commit `cd7ea2b`, see `docs/history.md`) — that shipped a speedup of the EXISTING ffmpeg/PNG pipeline this task still intends to replace; it doesn't reduce the scope or urgency of this task.** Blocked on `VideoEncoder`'s unresolved, unquantified hang risk on WKWebView/macOS: must be empirically bounded (repeated multi-session testing) or root-caused before this ships. Not production-viable yet. Windows/WebView2 and macOS arm64 not yet verified. See `docs/webcodecs-architecture-plan.md`'s B0 entry and its 2026-07-09 follow-ups (contradiction + Part 1/Part 2 reconciliation) for the full investigation: `VideoEncoder` works when tested (Part 2's end-to-end mux/round-trip proof passed cleanly) but carries an unexplained, unquantified hang risk — not production-viable until bounded.

## Deferred Polish Features

- Version snapshots (2 open design decisions before building: asset-restoration Design A vs B, and full-rewind-on-restore)
- Auto-captions (reuse Whisper transcript tokens as a timed text layer)
- Multi-user support — team accounts vs. staying single-user is still an open call; revisit if/when multi-user demand materializes

## Deferred Known Bugs

Found via real-app manual testing in the Tauri app (WebGL2 preview toggle ON), 2026-07-13. Distinct from the Ignored Low Risk Bugs below — those are dismissed as low-risk, these are tracked for a future audit.

- **Bug 3 — Combined lag (general WebGL2 preview performance):** general preview performance degradation reported this session alongside Bug 2 and the now-closed Bug 1 blend gap. No further repro detail captured yet. Status open, unaudited — needs a dedicated repro session to determine whether this is the same class of WKWebView `texImage2D` slow path Phase 3 Step 2 already fixed once, or a new distinct bottleneck, before it can be scoped. `src/hooks/useGlPreview.ts`, `src/services/gl/glCompositor.ts`

- **Regression — timeline segment edge-drag no longer tracks mouse movement:** noticed this session, unrelated to Bug 3 above (Bugs 1, 2, and 4 are already fixed and no longer listed here). Dragging a segment from its edge does not follow mouse movement proportionally — instead it jumps directly to a fixed length and locks that segment plus its neighbor. Unlocking both afterward snaps them back to their originally-synced position automatically, discarding the manual adjustment entirely. This differs from the previously-working drag behavior documented in CLAUDE.md's `App.tsx` file-map entry (`isResizingRef`, live-width-via-tagged-DOM-refs, single `applyDurationChange` commit on mouseup) and the D12 ghost-click fix history (`docs/history.md`, commit `be45b07`) — suspected regression, not a pre-existing bug. Root cause not yet audited; first step should be bisecting whether this appeared alongside the WebGL2 preview integration (Phase 3, commit `5a40cc6`) or is unrelated to it. `src/App.tsx`, `src/components/Timeline.tsx`

---

## SaaS Readiness Tasks

> Items required before public launch or multi-user distribution. Not scheduled — tracked here so they aren't forgotten.

- **Backend proxy for API keys** — Pexels/Pixabay/Coverr keys currently in JS bundle (VITE_ prefix). Required before public launch.
- **Auth layer** — No authentication; open access. Required for multi-user.
- **LGPL ffmpeg swap** — Current sidecar is GPL (libx264). Swap for LGPL-only build (OpenH264 or commercial x264 license) before public distribution.
- **4K export validation** — 1080p verified on macOS + Windows. 4K UI option exists but untested. Validate before advertising 4K support.
- **playbackSpeed UI re-expose** — Logic preserved in App.tsx; UI dropdown removed during 2026-06-17 BottomDrawer redesign. Re-expose as compact dropdown if user testing shows it's needed.
- **Restrict `fetch_url_bytes` with a domain allowlist (SSRF hardening)** — currently fetches any URL passed from the webview; acceptable for internal single-user use, required before public launch. `lib.rs`

---

## Key Invariants

Non-negotiables. Future work — especially the Architecture Shift active task — must not break these without a deliberate, documented decision.

- **(a) Sync timing is regression-locked.** `src/services/syncTiming.test.ts` (8 vitest tests, added in commit `05398f4` "lock sync timing pipeline with regression test") plus the `sync-known-good-2026-06-20` tag protect the sync/anchor timing pipeline. Tag message: *"Known-good single-click ms-correct sync. Baseline for per-slot re-sync work. Restore/bisect target if sync drifts."* Bisect or restore against this tag before reaching for a new fix if sync ever drifts again.
- **(b) Σ content-segment duration = voiceoverDuration.** This applies to content segments only — every `VideoSegment` in `project.segments` is content (no in-array headings exist as of Path B Phase 7, 2026-07-08). Total content-segment duration must always equal the voiceover's duration. Transition overlaps cancel pairwise by construction (Path B cross-fade design — see `docs/history.md`'s Decisions Log, 2026-05-25), so this holds without special-casing `App.tsx`. Headings are excluded entirely from this invariant — they own no timeline seconds (see (c)). This isn't theoretical: removing `splitAudio` in Heading Round 5 broke an earlier version of this invariant and cost 4 rounds of drift-bug fixes before headings were rebuilt as pure overlays.
- **(c) Headings are a separate top-level `HeadingOverlay[]` layer (`project.headings`), not segment-array entries.** Path B (docs/archived/path-b-heading-layer-plan.md) replaced the old in-array `isHeading`/`headingConfig` system entirely in Phase 7 (2026-07-08) — `VideoSegment` carries no heading fields anymore. A `HeadingOverlay` has a fixed absolute `time` that never moves on re-sync (a clamp+`needsReview` flag handles a re-sync that shrinks past it); it participates in no segment timing math and is composited on top of whichever segment(s) fall within its time range at render/export time.
- **(d) Transcription cache validity is keyed by file identity, not asset id.** `getFileIdentity(file) = \`${file.name}|${file.size}|${file.lastModified}\`` (`src/services/syncEngine.ts:216`), cached as `Project.lastTranscribedFileIdentity` (`src/types.ts:215`). Necessary because every file-stage event mints a fresh `Asset` id even when the user re-picks the identical file — id/reference equality can't catch a re-stage, but name+size+lastModified can.
- **(e) `anchorSource` provenance only ever moves one direction.** `'whisper'` = precise audio alignment; `'estimate'` = character-weight approximation that Whisper can still realign later. An anchor may be demoted `whisper → estimate` but is never promoted back, regardless of text changes (enforced by `syncTiming.test.ts`).
  - *Post-3c follow-up note — closed 2026-06-24 (post-3d-2):* `anchorSource` is confirmed effectively write-only — no production code branches on `'whisper'` vs `'estimate'`. Still written by `parseProjectData`, `applyAnchorBasedTiming` PASS 1, `distributeSegmentTimes`, and `handleInsertHeading` (PASS 2, the other writer, was deleted in 3d-2). Now documented directly in the `anchorSource` doc-comment in `src/types.ts`; no further cleanup planned.

---

## Decisions Log

| Date | Decision |
|---|---|
| 2026-07-19 | **Waveform rendered-image caching shipped and verified (commit `71fa4a0`); the broader reload-pacing problem remains open, unaffected by this fix.** Built a two-tier cache for RENDERED per-segment waveform thumbnail PNGs (distinct from `waveformStore.ts`'s existing numeric-peaks cache): an in-memory LRU mirror (Tier 1, asset-scoped, survives a same-session project switch) + a new IndexedDB store (Tier 2, `src/services/waveformImageCache.ts`, survives an app restart), keyed by assetId+blobSize+segmentId+startTime+duration. Paired with a pre-generation-bump identity gate in `App.tsx` (`waveformResidentRef` + `waveformStore.ts`'s new `peekWaveform` mirror) that short-circuits the apply-sync/reload-effect double-fire on an unchanged voiceover, and Phase D cleanup wiring (`deleteImagesForAsset`/`deleteAllImagesForProject`) into the voiceover-replace and project-delete paths so a removed asset's cached images don't leak. **Verified:** an instrumented trace (`__WF_INSTRUMENT__` flag, `src/instrumentFlag.ts`, localStorage-gated and off by default) confirmed 294/294 cache hits on a cold app restart of the 294-segment reference project; Phase D cleanup was manually confirmed in the real Tauri app. **Still open, NOT fixed by this work:** the reload-pacing sub-problem — a ~4s visible chunked build-in on that same 294-segment reload — remains unsolved. Four hypotheses for its cause were investigated and falsified against real trace data this session; see `docs/waveform-image-cache-plan.md` Section 6 for the falsified-hypothesis record. Next step is Safari Web Inspector profiling, not yet done. `tsc --noEmit` clean, `vitest` **632/632**. |
| 2026-07-16 | **Whisper CPU-crash root cause corrected: it was AVX-512, not AVX2 — AVX2/FMA/F16C re-enabled for speed, only AVX-512 kept off. Supersedes the AVX2-blame framing in the prior (same-day) entry below.** Confirmed CPU info from the affected Windows test machine — an **Intel Core i3-12100F (12th-gen Alder Lake)** — reframes the whole diagnosis. Alder Lake has **AVX-512 physically fused off** by Intel but fully supports AVX, AVX2, FMA, and F16C (standard since ~2013). So the instruction set that actually faulted with `STATUS_ILLEGAL_INSTRUCTION` (`0xC000001D`) was **AVX-512**, not AVX2. Likely original mechanism: the pre-fix default `GGML_NATIVE=ON` built for whatever the GitHub Actions runner's CPU supported, and when a runner had AVX-512 the emitted binary crashed on any AVX-512-less consumer CPU (all Alder Lake, plus many others) — explaining why earlier installers worked and then one suddenly crashed with no relevant code change. **Correction applied (both Windows and macOS-Intel x86_64 build steps):** flags are now `GGML_NATIVE=OFF`, `GGML_AVX=ON`, `GGML_AVX2=ON`, `GGML_FMA=ON`, `GGML_F16C=ON`, `GGML_AVX512=OFF`. Re-enabling **AVX2** (not just AVX) is what restores near-original transcription speed — the two prior fix attempts (all-SIMD-off, then AVX+F16C-only) both left AVX2 off and stayed slow. `AVX512=OFF` keeps the actual crash instruction set out; with `NATIVE=OFF` it is never emitted, so the binary is safe even on the rare Intel Macs whose Xeon-W does support AVX-512. **MSVC nuance (unchanged, now working in our favor):** ggml defines `GGML_FMA`/`GGML_F16C` only for `NOT MSVC`, so those two flags are inert on Windows/MSVC — but `AVX2=ON` implies FMA+F16C on MSVC, so they're enabled regardless; on macOS (AppleClang) all four are honored directly. **Compatibility tradeoff (deliberate):** vs. the superseded AVX-only build, enabling AVX2 raises the CPU floor from ~2011 (Sandy Bridge) to ~2013 Haswell / ~2015–2017 AMD, and drops low-power Atom/Goldmont chips that have AVX but not AVX2 — accepted for this app's modern target machines. v1.9.1 pin unchanged. **Status: CI/build-only working-tree change** (`.github/workflows/build.yml`, `src-tauri/binaries/README.md`); `tsc --noEmit`/`vitest` **543/543**/`cargo check` all clean but **none exercise the CI/cmake changes**. **STILL UNVERIFIED on real hardware** — a real Windows (and ideally macOS Intel) build → install → transcription test is required to confirm both no-crash on the i3-12100F and speed recovery toward ~1 min. Until then this root-cause correction remains a well-supported hypothesis, not a proven fix. |
| 2026-07-16 | **Whisper transcription slowdown from the AVX-disable crash-fix resolved via Static AVX+F16C; multi-variant runtime dispatch audited and rejected.** *(Superseded in part by the entry above: the AVX2-blame framing here — "CPUs lacking AVX2", AVX2 kept off — was corrected once the i3-12100F CPU details identified AVX-512 as the true crash cause. The `GGML_CPU_ALL_VARIANTS` audit and v1.9.1 pin below remain valid.)* Background: the earlier Windows whisper AVX crash-fix (commit `7bd9871`) built whisper-cli with all SIMD off (`GGML_NATIVE=OFF` + `AVX`/`AVX2`/`AVX512`/`FMA`/`F16C=OFF`) to stop `STATUS_ILLEGAL_INSTRUCTION` (`0xC000001D`) on end-user CPUs lacking AVX2. That fixed the crash but as a known, accepted tradeoff regressed transcription ~15–20× (a ~1 min voiceover went to ~15–20 min) — the same all-off flags were also on the macOS Intel (x86_64) slice in `.github/workflows/build.yml`. **Audit:** `GGML_CPU_ALL_VARIANTS` (one binary containing every microarch CPU variant, best-fit chosen at runtime — the theoretical "fast *and* safe on any CPU" fix) was evaluated and **rejected** for this app: ggml enforces (via `FATAL_ERROR` in `ggml/src/CMakeLists.txt`) that it requires `GGML_BACKEND_DL=ON` + `BUILD_SHARED_LIBS=ON`, which turns the single static sidecar exe into ~10 loadable `ggml-cpu-*.dll` variants that must sit beside the exe and be `dlopen`ed by `ggml_backend_load_all()` — incompatible with Tauri's single-file `externalBin` sidecar model — **and** it rides on an open, unresolved whisper.cpp bug (**#2963**, silent load failure on Windows/MSVC: if a variant DLL fails to load, whisper-cli exits 0 with zero tokens = silent no-transcription). Priority order applied: *silent failure > slow success > crash*, so a build that might silently produce no transcript was ruled worse than the slow-but-safe status quo. **Shipped instead — Static AVX+F16C** on both the Windows and macOS-Intel build steps: `GGML_NATIVE=OFF`, `GGML_AVX=ON`, `GGML_F16C=ON`, `AVX2`/`AVX512`/`FMA=OFF`. Single static exe, no packaging change; recovers most of the lost speed; safe on any x86 CPU from ~2011–2012 onward. **MSVC nuance:** ggml only defines `GGML_FMA`/`GGML_F16C` for `NOT MSVC` (MSVC implies them via AVX2/AVX512), so `GGML_F16C=ON` is **inert on Windows/MSVC** — the Windows binary is effectively **AVX-only** (~2011 Sandy Bridge+), which is *broader* CPU coverage than the macOS AVX+F16C slice (~2012 Ivy Bridge+), with no downside. Only residual gap: still faults on pre-2011 CPUs and low-power Atom/Goldmont Celerons that lack AVX. **Reproducibility:** the whisper.cpp clone was also pinned from unpinned `master` to **v1.9.1** (commit `f049fff9`) in both `build.yml` and `src-tauri/binaries/README.md`, after verifying v1.9.1's `ggml/CMakeLists.txt` still exposes all these flags at the same names — an unpinned master could otherwise silently change flag names or behavior between builds. **Status: CI/build-only working-tree change** (`.github/workflows/build.yml`, `src-tauri/binaries/README.md`), **not committed**. Not exercised by `tsc --noEmit`/`vitest`/`cargo check` (none touch CI). **NOT yet verified** on a real Windows or macOS build → install → transcription-timing test — correctness (no crash, no silent zero-token exit) and speed (should return toward ~1 min, not 15–20) still need a live build. Note: `docs/webgl-architecture-plan.md` was checked and deliberately left untouched — it is scoped to the WebGL2 effects engine and has no build/whisper section; the Decisions Log here is the correct home for build/CI decisions. |
| 2026-07-14 | **Large-export Windows `STATUS_BREAKPOINT` crash root-caused + fixed at the save tail (not the frame loop).** Reproducible on Windows: WebView2's renderer died with `STATUS_BREAKPOINT` (Chromium's `__debugbreak()` OOM/`CHECK` guard — a native crash, uncatchable by try/catch or ErrorBoundary) on image-only exports of ~350–500 frames but not ~200–250, always at ~100% progress (post-frame-loop). **Root cause (audited before fixing):** the *tail* of export inflated the whole final MP4 through the renderer heap 5–6× — `exportPipeline.ts` read `export_final.mp4` back via `ffmpeg_read_file`, which Tauri serializes as a JSON `number[]` (~8× the file size in V8), then `useExport.ts` re-copied it as a `Uint8Array`, a `Blob`, an `arrayBuffer()`, and a ~1.37× base64 string before `save_bytes_to_disk`. Peak scaled with final-file size, which for images scales ~linearly with frame count — exactly matching the 200/350 threshold and the ~100% timing. The bounded `FrameEncoderPool` frame loop (`cd7ea2b`) was explicitly ruled out (in-flight capped at `pool.size+1`; footprint independent of frame count) and left untouched — the crash predated it. **Fix (permanent):** new Rust `save_session_file` command copies the finished MP4 straight from the session temp dir to the user's chosen path with `fs::copy` (not `rename` — handles cross-volume $TMPDIR→dest on Windows); `exportProject` now returns `{ ok:true, outputFile }` (session-relative name, file left in place) instead of a `Blob`, and `useExport.ts` copies via `TauriBackend.saveOutputToDisk` **before** teardown deletes the session dir. The final file's bytes never enter the renderer. `ffmpeg_read_file`/`save_bytes_to_disk` are retained (former still used for bounded per-segment reads in the untouched `segmentEncoder.ts`; latter now frontend-unused but kept as a general utility). Also stripped the unconditional per-frame `[timing]` `console.log` in `segmentEncoder.ts` (audit-flagged debug cruft). Step-0 grep confirmed the only consumer of `ExportResult.blob` was the save path, so no fallback Blob path was needed. `tsc --noEmit` clean, `vitest` **414/414**, `cargo check` clean. Files: `src-tauri/src/ffmpeg.rs`, `src-tauri/src/lib.rs`, `src/services/exportPipeline.ts`, `src/services/tauriFfmpeg.ts`, `src/services/ffmpegBackend.ts`, `src/hooks/useExport.ts`, `src/services/segmentEncoder.ts` (console.log strip only). |
| 2026-06-28 | Windows dev environment: vcvars64.bat must be sourced before every cargo invocation on this machine (MSVC toolchain at custom D:\VSBuildTools2026b path, not on bare PATH). Permanent fix: .cargo/config.toml sets the linker path; dev.bat at project root sources vcvars64.bat then runs npm run tauri:dev — double-click to launch. Vite watcher configured to ignore src-tauri/target/** (EBUSY race condition on Windows). git identity set repo-scoped only on the Windows machine. |
| 2026-05-27 | **Branch strategy update:** Continuing short-lived feature branches, but merging directly to `main` with `git merge --no-ff` rather than via PR (single-developer workflow). |
| 2026-06-27 | **Billing block resolved + CI made manual-only.** The push-blocking billing issue is fixed — `origin/main` now tracks local HEAD again. To prevent recurring metered usage, the build workflow was switched to manual-only (`workflow_dispatch`, commit `e725a46`); CI no longer runs on push. Live thumbnail 3b (`23c8227`) is the first feature pushed under the restored flow. |
| 2026-07-01 | D12 root cause was a native ghost click racing ahead of React state, not a derived-state timing bug — a browser `click` synthesized right after `mouseup` can hit-test onto a completely different element than the one `mousedown` targeted if the pointer drifted during the gesture (exactly what a left-edge timeline resize does, since that handle's DOM position never tracks the cursor). Three earlier fix attempts targeting `currentSegment`/`useTransitionPreview` staleness were real but not the dominant cause, because native DOM event dispatch isn't gated by any React state/effect timing at all. **Reusable pattern:** when a drag-release intermittently triggers an unrelated click-handler side effect, suspect a native ghost-click before assuming a React state race — fix by arming a one-time, capture-phase `window` `click` listener in the drag's mouseup handler (only when the drag actually moved the pointer) that swallows the very next click before any bubble-phase React handler sees it. Commit `be45b07`. |
| 2026-07-07 | **Abandon further CSS/Canvas2D effects-engine patching; rebuild on WebGL/WebGPU instead.** Item 4's B4 sub-task (overlay parity — restoring `extraOverlays` during a live transition) was implemented and manually tested against `PreviewStage.tsx`/`useTransitionPreview.ts`, but did **not** fix the actually-reported symptoms (animation jump/disappear during transitions), and testing surfaced a new, unrelated bug: a React "Maximum update depth exceeded" infinite-render loop in `usePlayback.ts`. Rather than continue patching B4/B5 (or the CSS/Canvas2D effects engine generally, given this was already the second structural surprise after Phase B's `VideoEncoder` dead end), the decision was made to abandon the current DOM/CSS + Canvas2D rendering approach entirely and rebuild the effects engine on WebGL/WebGPU — see the new Active Tasks entry for scope (3 transitions + 2 animations, deliberately small first cut). All uncommitted B4 work was discarded via targeted `git restore` on `PreviewStage.tsx` and `useTransitionPreview.ts`, confirmed back to clean B3 state (commit `10410f0`; 162/162 tests passing at the time, now 216/216 with D16 on top). B1-B3 remain shipped and unaffected — Item 4's original frozen-frame symptom stays fixed. Full status recorded in `docs/webcodecs-architecture-plan.md`'s Item 4 entry. |
| 2026-07-09 | **Legacy `<video>` fallback audited — code kept, cold-start-freeze bug dropped from tracking.** Audited whether the pre-WebCodecs dual-`<video>`-slot preview path (used only when `isWebCodecsPreviewSupported()` is false — i.e. WKWebView below macOS 13.3/Safari 16.4, or non-evergreen WebView2) is safe to delete now that WebCodecs is the default. Decision: **keep the fallback code itself** — it's still the only working preview path on old macOS installs (Tauri's bundle default `minimumSystemVersion` is 10.13, well below the 13.3 WebCodecs floor, and this app ships an Intel macOS binary where pre-Ventura hardware is realistic), and there's no telemetry in this offline desktop app to prove otherwise. Separately, **stop tracking the "preview video cold-start clock freeze" bug** (formerly in Deferred Known Bugs) as an open item — it has never been reported by a real user, and it only affects that narrow, already-deprioritized fallback case. It was investigated and documented at the time in a standalone `docs/bugs/preview-cold-start-clock-freeze.md` writeup, but that file no longer exists anywhere in the repo; the WebCodecs migration section of `docs/webcodecs-architecture-plan.md` still references this bug as the "Origin of this effort" and remains the accurate surviving record of its context and resolution. |
| 2026-07-13 | **Bug 2 — WebGL2 preview zoom sudden-jump during transitions — FIXED via per-layer transform restructure.** Root cause: a single `animScale` was applied to the already-blended A/B composite, sourced from whichever segment `currentTime` bounds-fell into (`findContainingSegment`) — which flips outgoing→incoming discontinuously at exactly the transition window's centered progress-0.5 point (the same midpoint the D7 centering fix established), producing a visible scale-snap. Masked by dip-to-black/dip-to-white's opaque full-frame cover (a solid color hides any rescale), clearly visible during cross-dissolve/light-leak (live footage blending through with no cover). Fix: restructured the WebGL2 compositor's render pass chain (`src/services/gl/glCompositor.ts`) to per-layer compositing, matching industry-standard compositor architecture (After Effects/Nuke/Resolve/ffmpeg filter-graph pattern) — each segment's zoom is now applied to its own content independently BEFORE blending (prep slot A → prep slot B → blend → grade), not as one shared scalar applied after. `CompositeParams` (`src/services/gl/compositeParams.ts`) gained `animScaleA`/`animScaleB` (replacing the single `animScale`); `resolveAnimScale` itself unchanged, now called once per segment. One new render target added (3 FBOs peak, up from 2). The transition programs (cross-dissolve/dip/light-leak) now sample pre-rendered upright FBOs instead of raw uploads, requiring a vertex-shader reassignment (flipped → straight) — the same class of mock-invisible flip-parity risk as the Phase 2 pass-chain bug (`a92fc9a`), closed out via real-device manual verification rather than test-suite confidence alone. **Side effect found and corrected:** light-leak's procedural bloom center constant (`src/services/gl/shaders.ts`) was tuned for the old flipped orientation; its Y component was flipped (0.25 → 0.75) to restore the bloom's original top-right screen position under the new straight-VS sampling — the X component and the falloff/shaping math (`progress*(1-progress)*4`) were left untouched. **Manually verified by the user in the real Tauri app** (WebGL2 preview toggle ON): the zoom pop is confirmed gone; orientation, object-cover crop, and (after the Y-flip correction) light-leak's bloom position all confirmed correct, across cross-dissolve and light-leak with zoom segments on both sides of the transition. `tsc --noEmit` clean, `vitest` **409/409** (up from 371 — new per-layer zoom-continuity, lazy-3rd-render-target, and flip-parity tests). This closes the Bug 2 entry that was in Deferred Known Bugs; Phase 3's zoom-across-a-transitioning-boundary checklist item is now satisfied — see `docs/webgl-architecture-plan.md`'s Phase 3 section. Bug 3, Bug 4, and the timeline edge-drag regression remain open and unaudited. |
| 2026-07-13 | **Bug 4 — Black screen on WebGL2 preview cold load — FIXED via canvas-mount-aware effect dependencies.** Root cause: `useGlPreview.ts`'s GL-context-acquisition `useLayoutEffect` was keyed only on `[enabled]`. On a cold app launch with the dev toggle's `localStorage` value already persisted `true`, `enabled` is `true` on this hook's very first render — but the GL `<canvas>` itself only mounts once `currentSegment` hydrates (async project load), so that first effect invocation found `canvasRef.current` still `null`, bailed silently with no cleanup registered, and never fired again, since `enabled` never changed value a second time in that session — a plain `useRef`'s `.current` assignment doesn't participate in React's effect-dependency diffing, so the canvas mounting later couldn't retrigger it. This is a genuine ref-timing race, structurally distinct from the GPU/hardware-decoder cold-start theory investigated earlier in the same audit chain (that theory was ruled out once real-Tauri testing showed toggling the dev flag OFF then back ON — with no reload — reliably recovered the preview; a decoder-pool session cache bug found during that same investigation was fixed separately and is unrelated to this root cause, see the still-uncommitted `videoDecoderPool.ts` change). Fix: replaced the plain `useRef` canvas ref with a `useCallback` callback-ref (`canvasNode` state, internal to the hook, exposed as `UseGlPreviewResult.canvasRef`); added `canvasNode` to both the GL-init effect's and the per-tick render effect's dependency arrays, so a canvas that mounts after `enabled` was already true correctly retriggers both, regardless of prior `enabled` transitions. `PreviewStage.tsx` updated to attach `ref={glPreview.canvasRef}` instead of holding its own `glCanvasRef` — the only other file touched. `tsc --noEmit` clean, `vitest` **414/414** (up from 409 — 4 new tests). **Honest test-coverage note:** the new tests are pure re-statements of React's documented dependency-array shallow-compare (`Object.is` per entry) applied to the exact before/after shapes this fix changes — they do NOT render the hook or mechanically prove React itself re-invokes the effect, because this repo has no jsdom/happy-dom/@testing-library/react/react-test-renderer (confirmed absent from `node_modules`; same limitation this codebase has flagged before for `useWebCodecsPreview.test.ts`/`useGlPreview.test.ts`). **Verification was manual, in the real Tauri app**, not automated: user confirmed both a fresh cold load and the paused-cold-load variant (toggle already ON, project opened while paused, never played) now render correctly with no reload or toggle workaround needed. Closes the Bug 4 entry that was in Deferred Known Bugs. |

---

## Open Questions

---

## Quick Stats

| Metric | Value |
|---|---|
| `src/App.tsx` LOC | 2,962 (was 2,838 prior to Effects Tab Rebuild Steps 5–7) |
| Project persistence | Per-project scoped: `kinetix:project:{id}:v1` + registry `kinetix:projects:v1` in localStorage (legacy single-project key `kinetix:project:v1` retained for one-time migration only) |
| IndexedDB | `kinetix-assets` DB v2, store `assets-v2`, compound keyPath `['projectId','id']` (legacy v1 store retained for migration) |
| Total dependencies | 6 prod + 12 dev |
| Export codec | H.264 video + AAC audio, MP4 container |
| Export engine | Native ffmpeg sidecar (evermeet.cx 8.1.1 static build, GPL) via Tauri `tauri-plugin-shell` |
| Export speed (1080p/30fps) | **Stale — pending re-measurement.** Figures below predate the 2026-07-09 worker-pool PNG encode + raw-binary IPC write speedup (commit `cd7ea2b`, see `docs/history.md`); no post-fix benchmark has been run yet. macOS Intel (x86_64): ~10× realtime (120s for 12s of output) as of the base64-IPC-only pipeline; Windows: ~6× realtime (6 min per 1 min of video, measured on brother's PC); macOS arm64: pending measurement |
| Frontend bundle size | 505.86 kB / 152.74 kB gzip main bundle (measured 2026-06-22; no wasm in bundle — ffmpeg is a sidecar binary) |
| Lazy chunks | StockSearchModal 8.79 kB · jszip 95.87 kB |
| ffmpeg sidecar binaries | 76 MB (x86_64-apple-darwin), 48 MB (aarch64-apple-darwin), 97 MB (x86_64-pc-windows-msvc) — all gitignored; see `src-tauri/binaries/README.md` |
| Transition enum values in UI | 10 (only implemented transitions shown) |
| Filter names in UI | 26 (only implemented filters shown) |
| AnimationType values rendered in export | 12 (all applied via `canvasAnimations.ts`) |

---

## Ignored Low Risk Bugs

Low/no-risk — intentionally not scheduled. Revisit only if a user reports impact.

- **D11 — Preview letterboxing in normal view:** the preview stage shows letterbox bars in the non-fullscreen layout; under-documented placeholder behavior, not a regression. `PreviewStage.tsx`
- **D13 — Export cancel doesn't kill the in-flight ffmpeg subprocess:** the generation counter and session teardown fire immediately, but the running `ffmpeg_exec` sidecar continues to completion against the torn-down temp dir; the resulting error is swallowed. `useExport.ts`, `ffmpeg.rs`
- **D15 — Timeline scroll-restore assumes default zoom:** the one-shot reload scroll restore (`34206ee`) applies a raw persisted `scrollLeft` pixel value that's only valid at `sliderT = 0.5` (the value it's reset to on every `project.id` change); `sliderT` itself is never persisted. If a user zooms before reloading, the saved pixel offset maps to a different timeline position after reload and gets silently clamped by `maxScroll` — no crash, just minor scroll drift. Full fix would require persisting `sliderT` alongside `timelineScrollLeft`. `Timeline.tsx`, `App.tsx`
